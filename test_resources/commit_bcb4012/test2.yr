mod test2;
import std::io;

class A {

    let _a : i32 = 8;

    pub self () {}
    
    pub def foo (self) {
        println (self._a);
    }

    pub def bar (mut self, x : i32) {
        println (self._a, " ", x);
    }

}

def main () {
    let dmut a = A::new ();
    a:.foo ();
    a.bar (12);
}   
