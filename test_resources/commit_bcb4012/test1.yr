mod test1;
use std::io;

class A {

    let _a : i32 = 8;

    pub self () {
        ;
    }
    
    pub fn foo (self) {
        println (self._a);
    }

    pub fn bar (mut self, x : i32) {
        println (self._a, " ", x);
    }

}

fn main () {
    let dmut a = A::new ();
    a.foo ();
    a:.bar (12);
}   
