// copy propagation and temporary coalescing (YMI-78): the temporaries chain one
// into the next, nothing else reads them
fn foo (x : i32)-> i32 {
    let a = x;
    let b = a;
    let c = b;

    c
}
