// a panic the user wrote is no bounds check (YMI-20): its guard is kept, and no
// access is reported out of bounds
fn foo (x : bool)-> i32 {
    let mut n = 3us;
    if (x) { n = 4us; }
    if (n >= 2us) { panic; }

    0
}
