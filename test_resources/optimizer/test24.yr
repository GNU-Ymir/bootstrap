// concatenation fusion (YMI-18): the chain allocates its result once, whichever
// side of a concatenation the intermediate results are on
fn foo (a: [i32], b: [i32], c: [i32])-> [i32] {
    a ~ b ~ c
}

fn bar (a: [i32], b: [i32], c: [i32], d: [i32])-> [i32] {
    (a ~ b) ~ (c ~ d)
}
