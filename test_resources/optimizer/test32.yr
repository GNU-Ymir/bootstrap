// no bounds check elimination on an address taken (YMI-20): bar may give the
// slice any length
fn bar (ref dmut a : [i32]) {
    a = copy [1];
}

fn foo ()-> i32 {
    let dmut a = copy [1, 2, 3];
    bar (ref a);

    a [2]
}
