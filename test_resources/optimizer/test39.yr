// constant folding (YMI-21): the byte offset of a constant index is folded, and
// an offset of zero leaves the pointer itself
fn foo (a : [i32])-> i32 {
    a [10]
}

fn bar (a : [i32])-> [i32] {
    a [0 .. 7]
}
