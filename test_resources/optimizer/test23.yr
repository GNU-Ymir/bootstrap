// stack promotion (YMI-18): the allocation of a length known at compile time
// never leaves the frame, it becomes an array of the frame
fn foo (n: i32)-> i32 {
    let a = copy [i * 2 for i in 0 .. 8];
    let b = copy [i * 2 for i in 0 .. n];

    a[3] + b[0]
}
