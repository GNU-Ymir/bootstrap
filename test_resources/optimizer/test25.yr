// no concatenation fusion across a call (YMI-18): `bar` is evaluated between the
// two concatenations, and may write what the first one read
fn bar ()-> [i32] {
    copy [1]
}

fn foo (a: [i32], b: [i32])-> [i32] {
    a ~ b ~ bar ()
}
