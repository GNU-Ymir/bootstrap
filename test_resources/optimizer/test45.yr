// a panic the user guards with a propagated literal (YMI-21): the guard is
// folded, the panic is left unconditional, and no access is reported out of bounds
fn foo ()-> i32 {
    let mut n = 3us;
    if (n >= 2us) { panic; }

    0
}
