// slice bounds check elimination (YMI-20): the length the slice was built with
// and the index are both known here, the check the expander inserts is always false
fn foo ()-> i32 {
    let dmut a = copy [1, 2, 3];

    a [1]
}
