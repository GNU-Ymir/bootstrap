// slice bounds check elimination (YMI-81): the length of the array and the index
// are both known here, the check the expander inserts is always false
fn foo ()-> i32 {
    let a = [1, 2, 3];

    a [1]
}
