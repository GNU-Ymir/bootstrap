// no elision of a copy of an array read afterwards (YMI-18): writing the copy
// must not be seen through the array
fn foo (i: usize)-> i32 {
    let dmut a = [1, 2, 3];
    let dmut b = copy a;
    b[0] = 9;

    a[i]
}
