// bounds check elimination under a guard (YMI-20): the access is only reached
// when the index is below the length
fn foo (a : [i32], i : usize)-> i32 {
    if i < a.len {
        return a [i];
    }

    0
}
