// dead code elimination and CFG cleanup (YMI-79): the store to 'dead' is never
// read, and the block behind the return is never entered
fn foo (x : i32)-> i32 {
    let mut dead = 42;

    if x > 0 {
        return x;
    }

    dead = 12;
    0
}
