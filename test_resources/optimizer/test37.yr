// no bounds check folding on floats (YMI-20): x < y not taken does not prove
// y <= x, both are false when one of them is NaN
fn foo (x : f64, y : f64)-> i32 {
    if (x < y) return 1;
    if (x >= y) panic;

    0
}
