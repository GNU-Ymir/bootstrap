// runtime call elision and escape based stack promotion (YMI-83): the copy of the
// slice never leaves the frame, and nothing writes through it
fn foo ()-> i32 {
    let a = [1, 2, 3];
    let b = copy a;

    b [0]
}
