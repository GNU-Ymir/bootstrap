// devirtualization and indirect call promotion (YMI-82): the dynamic type of the
// receiver is the type it was allocated with, the vtable lookup is not needed
class A {
    pub self () {}
    pub fn foo (self)-> i32 { 7 }
}

fn foo ()-> i32 {
    let a = copy A ();

    a.foo ()
}
