// bounds check elimination in a loop (YMI-20): the loop bound is the length of
// the slice it walks, every index it gives is in bounds
fn foo (a : [i32])-> i32 {
    let mut s = 0;
    for i in 0us .. a.len {
        s += a [i];
    }

    s
}
