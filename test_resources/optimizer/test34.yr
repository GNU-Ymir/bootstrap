// bounds check elimination over two paths (YMI-20): the slice holds 2 or 3
// elements, a[1] is in bounds on both, a[2] only on one
fn foo (x : bool)-> i32 {
    let mut a = copy [1, 2, 3];
    if (x) { a = copy [4, 5]; }

    a [1] + a [2]
}
