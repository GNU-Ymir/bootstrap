// no redundant bounds check across a reassignment (YMI-20): the second access
// reads another slice than the first
fn foo (x : [i32], y : [i32], i : usize)-> i32 {
    let mut a = x;
    let r = a [i];
    a = y;

    r + a [i]
}
