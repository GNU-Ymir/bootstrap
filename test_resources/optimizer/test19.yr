// runtime call elision (YMI-18): the copy of the literal never leaves the frame
// and nothing else reads the literal, the copy is the literal itself
fn foo (i: usize)-> i32 {
    let dmut b = copy [1, 2, 3];
    b[0] = 9;

    b[i]
}
