// algebraic identities (YMI-21): an operation that gives back one of its operands,
// or a constant whatever its operand is, is replaced by it
fn foo (x : i32)-> i32 {
    let mut z = 0, mut o = 1;
    (x + z) * o - (x - x) + (x ^ x) - (x * z)
}

fn bar (x : i32)-> i32 {
    - (- x)
}
