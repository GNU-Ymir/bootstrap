// no stack promotion of a slice appended to (YMI-18): the runtime grows its own
// block in place, which the other holders of the block then see
fn foo ()-> i32 {
    let dmut a = copy [0; 3];
    let dmut b = alias a;
    a ~= copy [1];
    a[0] = 7;

    b[0]
}
