struct
| a : i32
| b : i32
 -> X;

fn foo (a : i32) {
    a;
}

fn main () {
    let a = X (1, 2);
    cte for i in 0us .. X::field_infos.len {
        foo (__pragma!field (a, cte {X::field_infos [i].name}));
    }
}
