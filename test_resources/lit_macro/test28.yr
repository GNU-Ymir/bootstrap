/*
 * The content generated by a macro is syntactically correct, but does not
 * pass the validation
 */

macro mismatch {
    self (a=__operand) skips (" ") #{
        #(a) + "not an int"
    }
}

fn foo () {
    let x = mismatch#{ 1 };
    x;
}
