/*
 * Two constructors of the same macro accepting the content of the call,
 * the call is thus ambiguous
 */

macro amb {
    self (a=__operand) skips (" ") #{
        #(a)
    }

    self (b=__literal) skips (" ") #{
        #(b)
    }
}

fn foo ()-> i32 {
    amb#{ 1 }
}
