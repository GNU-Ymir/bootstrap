/*
 * A '__ident' rule called with an identifier it is not restricted to
 */

macro named {
    self (i=__ident["truc" | "bidule"] "=" v=__literal) skips (" ") #{
        let #(i) = #(v);
        #(i)
    }
}

fn foo ()-> i32 {
    named#{ machin = 12 }
}
