/*
 * A '__ident' rule restricted to a list of identifiers
 */

macro named {
    self (i=__ident["truc" | "bidule"] "=" v=__literal) skips (" ") #{
        let #(i) = #(v);
        #(i)
    }
}

fn foo ()-> i32 {
    named#{ truc = 12 }
}

fn bar ()-> i32 {
    named#{ bidule = 8 }
}
