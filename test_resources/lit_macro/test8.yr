/*
 * A macro whose body refers to a variable that is not captured by
 * the rule of the constructor
 */

macro wrong {
    self (a=__operand) skips (" ") #{
        #(a) + #(b)
    }
}

fn foo () {
    let x = wrong#{ 1 };

    x;
}
