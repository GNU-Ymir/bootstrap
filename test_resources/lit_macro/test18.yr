/*
 * A named rule whose body is ';' generates nothing, it is only used to
 * validate a part of the content of the macro call
 */

macro kw {
    self (keyword v=__operand) skips (" ") #{
        #(v)
    }

    fn keyword ("value" ":") skips (" ");
}

fn foo ()-> i32 {
    kw#{ value : 12 }
}
