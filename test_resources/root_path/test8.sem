frame: [weak] std::algorithm::comparison::max!{i32, i32}::max (a: i32, b: i32)-> i32 return if (a < b) b else a
frame: test8::main ()-> void {
    let a: i32 = std::algorithm::comparison::max!{i32, i32}::max (1, 2);
    <unit-value>
}
