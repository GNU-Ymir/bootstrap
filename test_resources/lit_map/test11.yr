// An array literal is not a map, whatever the type it is annotated with -- the
// suggestion is prose here on purpose, a copy around it makes a slice.
fn main () {
    let dmut _ : [i32 => i32] = [1, 2];
}
