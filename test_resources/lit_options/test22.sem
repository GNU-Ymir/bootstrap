frame: test22::foo (x: bool)-> mut [mut i32] return {
    if x throw core::exception::assertion::AssertError::self(new, ""s8);
    copy ({.len = 3us, .ptr = &([1, 2, 3])})
}
frame: test22::main ()-> void {
    let mut a: mut [mut i32] = if !try {
            {
                (test22::foo (false))?
            }
        } catch {
            let #_catch: mut &(mut core::exception::Exception)
            {
                {.ok = false, .error = #_catch}
            }
        }.hasValue copy ({.len = 3us, .ptr = &([2, 3, 4])}) else alias (try {
                {
                    (test22::foo (false))?
                }
            } catch {
                let #_catch: mut &(mut core::exception::Exception)
                {
                    {.ok = false, .error = #_catch}
                }
            }.value);
    let mut _b_: mut [mut i32] = if !try {
            {
                (test22::foo (true))?
            }
        } catch {
            let #_catch: mut &(mut core::exception::Exception)
            {
                {.ok = false, .error = #_catch}
            }
        }.hasValue alias (a) else alias (try {
                {
                    (test22::foo (true))?
                }
            } catch {
                let #_catch: mut &(mut core::exception::Exception)
                {
                    {.ok = false, .error = #_catch}
                }
            }.value);
    test22::bar (_b_);
    <unit-value>
}
