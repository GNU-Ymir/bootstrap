fn foo(x: bool)-> dmut [i32]
    throws AssertError
{
    if (x) throw copy AssertError("");
    copy [1, 2, 3]
}

fn bar(x: [i32]);

fn main () {
    let dmut a = foo(false) ?? (copy [2, 3, 4]);
    let dmut _b_ = foo(true) ?? (alias a);
    bar(_b_);
}
