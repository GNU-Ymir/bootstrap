// the content throws, so the option answers on the throwing path, even though the
// break leaves the loop on the other one

fn foo (x : bool = false) throws AssertError;

fn main () {
    let _ = loop {
        let a = ({
            foo (x-> true);
            break 12;
        })?;

        a;
    };
}
