frame: test20::A::self (mut self: mut &(mut test20::A), next: (dg ()-> void)[nullable])-> void {
    if (next.func != null) (next.func)(next.closure);
    <unit-value>
}
frame: test20::B::self::_3_22 (#{CLOSURE-VARREF}: *((mut &(mut test20::B), (dg ()-> void)[nullable])))-> void {
    if (next.func != null) (next.func)(next.closure);
    <unit-value>
}
frame: test20::B::self (mut self: mut &(mut test20::B), next: (dg ()-> void)[nullable])-> void {
    test20::A::self(self, {&((self, next));(#{CLOSURE-VARREF}: *((mut &(mut test20::B), (dg ()-> void)[nullable])))-> void});
    <unit-value>
}
frame: test20::bar (x: bool)-> i32 return {
    if x throw core::exception::assertion::AssertError::self(new, ""s8);
    12
}
frame: test20::baz (x: bool)-> (&(test20::B))? return {
    if x {
        (test20::B::self(new))?
    } else none
}
frame: test20::main ()-> void {
    let _a_: i32 = if !try {
            {
                (test20::bar (false))?
            }
        } catch {
            let #_catch: mut &(mut core::exception::Exception)
            {
                {.ok = false, .error = #_catch}
            }
        }.hasValue 42 else try {
            {
                (test20::bar (false))?
            }
        } catch {
            let #_catch: mut &(mut core::exception::Exception)
            {
                {.ok = false, .error = #_catch}
            }
        }.value;
    let _b_: &(test20::A) = if !test20::baz (false).hasValue test20::A::self(new) else test20::baz (false).value;
    <unit-value>
}
