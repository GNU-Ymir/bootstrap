// a content that always throws is caught by the option, the exit it marks is the
// throw the operator answers, not a break or a return it could never reach

fn main () {
    let _a_ : void? = ({
        assert (false, "message");
    })?;
}
