fn foo (x : bool)-> i32? {
    if (x) {
        12?
    } else {
        none
    }
}

fn main () {
    let a = foo (false);
    let b = foo (true);
    let c = (13)?;

    c != (12)?;
    a != b;
    a != c;

    a == none;
    none == none;
    none < a;
    none < b;
    none < c;
}
