frame : test4::main ()-> void {
    let a : (i32)? = test4::foo (false);
    let b : (i32)? = test4::foo (true);
    let c : (i32)? = (13)?;
    true;
    if (a.hasValue && b.hasValue) (a.value != b.value) else if b.hasValue true else if a.hasValue true else false;
    if a.hasValue (a.value != 13) else true;
    if a.hasValue false else true;
    true;
    if a.hasValue true else false;
    if b.hasValue true else false;
    true;
    <unit-value>
}
frame : test4::foo (x : bool)-> (i32)? return if x (12)? else none
