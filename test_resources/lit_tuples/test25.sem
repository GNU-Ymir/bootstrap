frame: test25::main ()-> void {
    let mut a: mut [mut i32] = copy ({.len = 1us, .ptr = &([1])});
    let mut b: mut [mut i32] = copy ({.len = 1us, .ptr = &([2])});
    #{
        a = alias ((copy ({.len = 1us, .ptr = &([3])}), copy ({.len = 1us, .ptr = &([4])})).0);
        b = alias ((copy ({.len = 1us, .ptr = &([3])}), copy ({.len = 1us, .ptr = &([4])})).1);
        <unit-value>
    #};
    let mut x: mut [mut i32] = copy ({.len = 1us, .ptr = &([5])});
    let mut y: mut [mut i32] = copy ({.len = 1us, .ptr = &([6])});
    #{
        a = alias ((alias (x), alias (y)).0);
        b = alias ((alias (x), alias (y)).1);
        <unit-value>
    #};
    #{
        a = alias ((copy ({.len = 1us, .ptr = &([7])}), copy ({.len = 1us, .ptr = &([8])})).0);
        b = alias ((copy ({.len = 1us, .ptr = &([7])}), copy ({.len = 1us, .ptr = &([8])})).1);
        <unit-value>
    #};
    let mut t: (mut [mut i32], mut [mut i32]) = (copy ({.len = 1us, .ptr = &([9])}), copy ({.len = 1us, .ptr = &([10])}));
    #{
        a = alias (t.0);
        b = alias (t.1);
        <unit-value>
    #};
    <unit-value>
}
