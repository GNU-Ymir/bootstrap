mod test12;
use std::io;

fn foo ()-> i32 {
    println ("ici");
    12
}

fn bar (a : i32) {
    println ("bar");
    a;
}

fn main () {
    let a = (foo(),);
    let (b, c) = (expand a, foo ());
    bar (expand a);
    bar (b);
    bar (c);
}
