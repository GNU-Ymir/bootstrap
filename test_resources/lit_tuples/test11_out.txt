frame : test11::bar ()-> void {
    #{
        let mut a : mut i32 = 1;
        let b : c8 = 't'c8;
        let c : f32 = 12f
    #};
    core::exception::abort ((((a == 1) && (b == 't'c8)) && (c == 12f)), ""s8);
    #{
        let mut d : mut i32 = 1;
        let mut e : mut [mut f32 ; 2us] = [1f, 2f]
    #};
    d;
    e;
    <unit-value>
}
frame : test11::foo ()-> void {
    #{
        let a : i32 = 1;
        let b : (i32, i32) = (2, 3)
    #};
    core::exception::abort ((a == 1), ""s8);
    core::exception::abort (((b.0 == 2) && (b.1 == 3)), ""s8);
    #{
        let c : i32 = 1;
        let d : (i32,) = (2,)
    #};
    core::exception::abort ((c == 1), ""s8);
    core::exception::abort ((d.0 == 2), ""s8);
    #{
        let e : i32 = 1;
        let f : (i32, f32) = (2, 3f)
    #};
    e;
    f;
    <unit-value>
}
