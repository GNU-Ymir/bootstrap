frame: test18::S::self (mut ref self: mut test18::S, x: i32, y: i32)-> void {
    {
        self.x = x;
        self.y = y
    };
    <unit-value>
}
frame: test18::foo ()-> (i32, i32) return (1, 2)
frame: test18::main ()-> void {
    let mut s: mut test18::S = test18::S::self(new, 0, 0);
    let mut t: mut [mut i32] = copy ({.len = 2us, .ptr = &([0, 0])});
    let mut c: mut i32 = 0;
    #{
        s.x = test18::foo ().0;
        #{
            if (t.len <= 0us) panic;
            t [0us]
        #} = test18::foo ().1;
        <unit-value>
    #};
    #{
        #{
            s.y = (test18::foo (), 3).0.0;
            #{
                if (t.len <= 1us) panic;
                t [1us]
            #} = (test18::foo (), 3).0.1;
            <unit-value>
        #};
        c = (test18::foo (), 3).1;
        <unit-value>
    #};
    <unit-value>
}
