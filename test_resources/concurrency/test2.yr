// the handle of a future returning borrowable data is copied immutably without alias
fn rows ()-> dmut [i32] {
    copy [1, 2]
}

fn main () {
    let dmut f = spawn rows ();
    let f2 = f;
    let f3: future-> [i32] = f;
    f2;
    f3;
}
