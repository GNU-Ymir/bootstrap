// an immutable copy of a future handle does not return a mutable value
fn rows ()-> dmut [i32] {
    copy [1, 2]
}

fn main () {
    let dmut f = spawn rows ();
    let f2 = f;
    let dmut x = f2.value;
    x;
}
