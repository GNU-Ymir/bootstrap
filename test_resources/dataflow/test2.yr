fn bar ()-> i32
    throws AssertError;

fn foo (x : i32)-> i32 {
    let msg = x * 2; // read by the handler only

    let r = {
        bar ()
    } catch {
        _ : &AssertError => { msg }
    };

    r
}
