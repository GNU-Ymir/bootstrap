// a derived instance bound by reference to its base class in a pattern
class Base {
    pub self () {}
}

class Derived over Base {
    pub self () with super () {}
}

fn main () {
    let d = copy Derived ();
    match d {
        ref b : &Base => { let _ = b; }
        _ => {}
    }
}
