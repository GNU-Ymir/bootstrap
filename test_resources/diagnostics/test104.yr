// a function pointer made from the method of a record, outside a copy
record Counter {
    pub self () {}
    pub fn get (self)-> i32 { 1 }
}

fn main () {
    let c = Counter ();
    let _ = &c.get;
}
