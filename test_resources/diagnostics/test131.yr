// a record pattern with a field given by position
record Point {
    pub let x : i32;
    pub self (x : i32) with x = x {}
}

fn main () {
    let p = Point (1);
    match p {
        Point (1) => {}
        _ => {}
    }
}
