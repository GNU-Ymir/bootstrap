// a function pointer made from the method of a record, inside a deep copy
record Counter {
    pub self () {}
    pub fn get (self)-> i32 { 1 }
}

fn main () {
    let c = Counter ();
    let _ = dcopy &c.get;
}
