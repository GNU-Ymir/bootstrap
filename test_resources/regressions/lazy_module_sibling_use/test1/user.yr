in user;

use test1::{base, helper};

// reaching Base is what forces the lazily-declared 'base' to be loaded mid-validation
pub fn run ()-> i32 {
    let b = copy Base ();
    b.describe (copy Helper ())
}
