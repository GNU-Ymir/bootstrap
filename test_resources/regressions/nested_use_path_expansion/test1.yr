/*
 * The '{ ... }' expansion of a 'use' path must keep every segment that
 * precedes the brace, not only the first one : 'outer::inner::{a, b}'
 * imports 'outer::inner::a' and 'outer::inner::b'.
 */

in test1;

use outer::inner::{a, b};

fn main ()-> i32 {
    a::fa () + b::fb ()
}
