in test1;

fn cut (n : u32)-> [[i32]] {
    let mut result : [mut [i32]] = copy [[] for _ in 0 .. n];
    result
}

fn main ()
    throws AssertError
{
    let x = cut (3u32);
    assert (x.len == 3us);
}
