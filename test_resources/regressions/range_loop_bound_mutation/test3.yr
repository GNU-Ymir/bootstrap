// the comprehension reads the start bound once, before its loop
fn next (ref mut a: i32)-> i32 {
    a += 1;
    a
}

fn main () {
    let mut a = 0;
    let _ = copy [i + next (ref a) for i in a .. 5];
}
