frame: test3::main ()-> void {
    let mut a: mut i32 = 0;
    let _: [i32] = {
            let mut #_iter: usize = 0us;
            let #_nb_iters: usize = if (a > 5) 0 else if (((5 - a) + a) != 5) ((5 - a) + 1) else (5 - a);
            let #_fst: i32 = a;
            let mut i: i32 = 0;
            let mut #_value: mut [mut i32] = copy [<unit-value> ; if (a > 5) 0 else if (((5 - a) + a) != 5) ((5 - a) + 1) else (5 - a)];
            while (#_iter < #_nb_iters) {
                i = (#_fst + cast!{i32}(#_iter));
                #_value [#_iter] = (i + test3::next (ref (a)));
                #_iter = (#_iter + 1us)
            };
            #_value
        };
    <unit-value>
}
frame: test3::next (mut ref a: mut i32)-> i32 return {
    a = (a + 1);
    a
}
