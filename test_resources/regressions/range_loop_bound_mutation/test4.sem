frame: test4::main ()-> void {
    let n: i32 = 5;
    {
        let mut #_iter: usize = 0us;
        let #_nb_iters: usize = 5us;
        while (#_iter < #_nb_iters) {
            let mut i: i32 = 0;
            i = cast!{i32}(#_iter);
            {
                i;
                <unit-value>
            }
        } step #_iter = (#_iter + 1us)
    }
}
