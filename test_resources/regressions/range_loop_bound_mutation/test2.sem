frame: test2::main ()-> void {
    let mut r: mut (..mut i32) = (0 ..[false] 5).step_by(1);
    {
        let mut #_iter: usize = 0us;
        let #_nb_iters: usize = {
                let #_range: (mut i32, mut i32, mut i32) = (r.fst, r.scd, r.step);
                let #_val: (mut i32, mut i32, mut i32, mut i32) = if (#_range.2 < 0) (-#_range.2, #_range.1, #_range.0, (#_range.0 - #_range.1)) else (#_range.2, #_range.0, #_range.1, (#_range.1 - #_range.0));
                let #_nbAll: mut i32 = (#_val.3 / #_val.0);
                if (#_val.1 > #_val.2) 0 else if (#_nbAll == 18446744073709551615) #_nbAll else if ((((#_nbAll * #_val.0) + #_val.1) != #_val.2) || r.contains) (#_nbAll + 1) else #_nbAll
            };
        let #_fst: i32 = r.fst;
        let #_step: i32 = r.step;
        while (#_iter < #_nb_iters) {
            let mut i: i32 = 0;
            i = (#_fst + cast!{i32}((#_iter * cast!{usize}(#_step))));
            {
                r = (10 ..[false] 20).step_by(1);
                i;
                <unit-value>
            }
        } step #_iter = (#_iter + 1us)
    }
}
