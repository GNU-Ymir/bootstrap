frame: test1::main ()-> void {
    let mut a: mut i32 = 0;
    {
        let mut #_iter: usize = 0us;
        let #_nb_iters: usize = if (a > 5) 0 else if (((5 - a) + a) != 5) ((5 - a) + 1) else (5 - a);
        let #_fst: i32 = a;
        while (#_iter < #_nb_iters) {
            let mut i: i32 = 0;
            i = (#_fst + cast!{i32}(#_iter));
            {
                a = (a + 1);
                i;
                <unit-value>
            }
        } step #_iter = (#_iter + 1us)
    }
}
