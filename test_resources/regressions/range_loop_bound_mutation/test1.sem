frame: test1::main ()-> void {
    let mut a: mut i32 = 0;
    {
        let mut #_iter: usize = 0us;
        let #_nb_iters: usize = {
                let #_range: (mut i32, i32, i32) = (a, 5, if (a <= 5) 1 else -1);
                let #_val: (mut i32, mut i32, mut i32, mut i32) = if (#_range.2 < 0) (-#_range.2, #_range.1, #_range.0, (#_range.0 - #_range.1)) else (#_range.2, #_range.0, #_range.1, (#_range.1 - #_range.0));
                let #_nbAll: mut i32 = (#_val.3 / #_val.0);
                if (#_nbAll == 18446744073709551615) #_nbAll else if (((#_nbAll * #_val.0) + #_val.1) != #_val.2) (#_nbAll + 1) else #_nbAll
            };
        let #_fst: i32 = a;
        let #_step: i32 = if (a <= 5) 1 else -1;
        while (#_iter < #_nb_iters) {
            let mut i: i32 = 0;
            i = (#_fst + cast!{i32}((#_iter * cast!{usize}(#_step))));
            {
                a = (a + 1);
                i;
                <unit-value>
            }
        } step #_iter = (#_iter + 1us)
    }
}
