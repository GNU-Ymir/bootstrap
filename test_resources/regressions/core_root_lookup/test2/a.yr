in a;

use core;

// the class layout resolves core::types::info::TypeInfo from the root, this `use` makes
// nothing reachable that the module would not reach without it
pub class AClass {
    pub self () {}
}
