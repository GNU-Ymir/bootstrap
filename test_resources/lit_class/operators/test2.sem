frame: test2::A::self (mut self: mut &(mut test2::A), x: i32, next: (dg ()-> void)[nullable])-> void {
    {
        self._i = x;
        if (next.func != null) (next.func)(next.closure)
    };
    <unit-value>
}
frame: [weak] test2::A::opUnary!{"!"s8}::opUnary (self: &(test2::A))-> &(test2::A) return {
    let mut x: mut i32 = 1;
    {
        let mut #_iter: usize = 0us;
        let #_nb_iters: usize = if (1 > self._i) 0 else if ((self._i - 1) == 18446744073709551615) (self._i - 1) else ((self._i - 1) + 1);
        while (#_iter < #_nb_iters) {
            let mut i: i32 = 0;
            i = (1 + cast!{i32}(#_iter));
            {
                x = (x * i);
                <unit-value>
            }
        } step #_iter = (#_iter + 1us)
    };
    test2::A::self(new, x)
}
frame: [weak] test2::A::opUnary!{"-"s8}::opUnary (self: &(test2::A))-> &(test2::A) return {
    test2::A::self(new, -self._i)
}
frame: test2::main ()-> void {
    let a: &(test2::A) = test2::A::self(new, 10);
    let b: &(test2::A) = test2::A::opUnary!{"!"s8}::opUnary(a);
    let c: &(test2::A) = test2::A::opUnary!{"-"s8}::opUnary(a);
    <unit-value>
}
