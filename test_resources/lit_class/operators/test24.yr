// operands of the same class that differ in mutability still try the right operand
class A {
    pub self () {}

    pub fn opEquals (self, dmut _ : &A)-> bool {
        true
    }
}

fn main () {
    let a = copy A ();
    let dmut b = copy A ();
    let _ : bool = (alias b) == a;
}
