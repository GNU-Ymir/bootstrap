// the key type of minBy comes from the body of the lambda
fn minBy {K, T of [U], U} (a: T, f: dg (U)-> K)-> K {
    f (a [0])
}

fn main () {
    let a = copy ["ab", "c"];
    let k: usize = a.minBy (|x| => x.len);
    k;
}
