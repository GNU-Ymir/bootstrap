// the same deduction for a function pointer parameter
fn map {V, U} (a: [U], f: fn (U)-> V)-> [V] {
    copy [f (i) for i in a]
}

fn main () {
    let a = copy [1, 2, 3];
    let b: [i64] = a.map (|x| => cast!i64 (x));
    b;
}
