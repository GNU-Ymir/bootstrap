// the return type of an untyped lambda binds a template parameter found nowhere else
fn map {V, T of [U], U} (a: T, f: dg (U)-> V)-> [V] {
    copy [f (i) for i in a]
}

fn main () {
    let a = copy [1, 2, 3];
    let b: [i32] = a.map (|x| => x * 2);
    b;
}
