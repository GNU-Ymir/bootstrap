// the lambda parameter is the element type of a generator argument
yield src ()-> i32 {
    yield 1;
    yield 2;
}

fn map {V, T of (yield-> U), U} (a: T, f: dg (U)-> V)-> [V] {
    copy [f (i) for i in a]
}

fn main () {
    let b: [bool] = src ().map (|x| => x > 1);
    b;
}
