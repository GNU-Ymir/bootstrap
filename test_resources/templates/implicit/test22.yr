// the same deduction from a closure
fn map {V, T of [U], U} (a: T, f: dg (U)-> V)-> [V] {
    copy [f (i) for i in a]
}

fn main () {
    let a = copy [1, 2, 3];
    let z = 3.0;
    let b: [f64] = a.map (copy |x| => cast!f64 (x) * z);
    b;
}
