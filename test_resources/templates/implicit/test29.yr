// an error in the body of the lambda is reported against the deduced parameter type
fn map {V, T of [U], U} (a: T, f: dg (U)-> V)-> [V] {
    copy [f (i) for i in a]
}

fn main () {
    let a = copy [1, 2, 3];
    a.map (|x| => x.foo);
}
