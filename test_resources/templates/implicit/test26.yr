// the return type of a lambda fixes the parameter type of the next one
fn chain {A, B, C} (a: A, f: dg (A)-> B, g: dg (B)-> C)-> C {
    g (f (a))
}

fn main () {
    let r: bool = chain (1, |x| => cast!f64 (x), |y| => y > 1.0);
    r;
}
