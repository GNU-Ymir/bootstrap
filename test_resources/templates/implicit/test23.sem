frame: [weak] test23::map!{bool, (yield-> i32), i32}::map (a: (yield-> i32), f: dg (i32)-> bool)-> [bool] return {
    {
        let #_iter: (yield-> i32) = a;
        let mut #_yielded: mut i32;
        let mut #_value: mut [mut bool] = copy [<unit-value> ; 0us];
        while (#_iter).next(#_yielded) {
            let i: i32 = #_yielded;
            #_value ~= {.len = 1us, .ptr = &([(f.func)(f.closure, i)])}
        };
        #_value
    }
}
frame: test23::src ()-> (yield-> i32) {
    yield 1;
    yield 2
}
frame: [weak] test23::main::_12_33 (#{CLOSURE-VARREF}: *(()), x: i32)-> bool return (x > 1)
frame: test23::main ()-> void {
    let b: [bool] = test23::map!{bool, (yield-> i32), i32}::map (test23::src (), {null;(#{CLOSURE-VARREF}: *(()), x: i32)-> bool});
    <unit-value>
}
