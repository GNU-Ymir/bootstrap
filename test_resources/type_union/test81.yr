// A comparison operator one of the members does not declare is not callable
// on the union.

class A {
    pub self () {}
    pub fn opEquals (self, o : i32)-> bool { o == 1 }
}

class B {
    pub self () {}
}

union U
| A
| B;

fn eq (u : &U)-> bool {
    u == 1
}
