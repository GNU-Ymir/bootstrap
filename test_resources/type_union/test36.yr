// A union of records is not converted back to the member it holds.

record A {
    pub let a : i32;

    pub self (a : i32)
        with a = a
    {}
}

record B {
    pub let b : f64;

    pub self (b : f64)
        with b = b
    {}
}

union U
| A
| B;

fn take (u : U)-> A {
    u
}
