// A union whose members agree on no method dispatches nothing, and the
// widening stores no vtable.

class A {
    pub self () {}
}

class B {
    pub self () {}
}

union U
| A
| B;

fn take (u : &U) {
    u;
}

fn main () {
    take (A ());
}
