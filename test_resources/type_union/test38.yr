// A union is not an instance of the base it declares, and does not convert to
// it either.

class Base {
    pub self () {}
}

class A over Base {
    pub self () {}
}

class B over Base {
    pub self () {}
}

union U over Base
| A
| B;

fn take (u : &U)-> &Base {
    u
}
