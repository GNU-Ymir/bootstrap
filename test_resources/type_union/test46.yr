// A default arm added to a match already covering every member is never
// evaluated.

record X {
    pub self () {}
}

record Y {
    pub self () {}
}

union U
| X
| Y;

fn main () {
    let u : U = X ();
    match u {
        x : X => { x; }
        y : Y => { y; }
        _ => {}
    }
}
