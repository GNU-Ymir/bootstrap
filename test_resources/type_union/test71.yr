// A trait implemented by the members with different arguments is not a trait
// the union implements.

trait R {I} {}

record A {
    impl R!i32;
}

record B {
    impl R!f32;
}

union U
| A
| B;

fn foo {T impl X!{I}, X, I} () {}

fn bar () {
    foo!{U} ();
}
