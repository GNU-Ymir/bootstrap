// A template parameter declared 'enum T' binds the enumeration itself, not the
// type it wraps.

enum X
| A = 1
| B = 2;

fn keep {enum T} (x : T)-> T {
    x
}

fn main () {
    let v : X = keep (X::A);
    let w : X = keep!{X} (X::B);
    v;
    w;
}
