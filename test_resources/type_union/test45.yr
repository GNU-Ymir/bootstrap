// A single member is tested by 'if let', which binds it for the block it guards.

class A {
    pub let a : i32;

    pub self (a : i32)
        with a = a
    {}
}

class B {
    pub self () {}
}

union U
| A
| B;

fn main () {
    let u : &U = A (1);
    if let a : &A = u {
        a.a;
    }
}
