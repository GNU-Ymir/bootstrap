frame: [weak] test60::A!{i32}::A::self (mut ref self: mut test60::A!{i32}::A, a: i32)-> void {
    self.a = a;
    <unit-value>
}
frame: test60::B::self (mut ref self: mut test60::B, b: f64)-> void {
    self.b = b;
    <unit-value>
}
frame: [weak] test60::foo!{test60::U!{i32}::U, i32}::foo (u: test60::U!{i32}::U)-> i32 return 0
frame: test60::main ()-> void {
    let u: test60::U!{i32}::U = test60::U!{i32}::U#0(test60::A!{i32}::A::self(new, 1));
    let x: i32 = test60::foo!{test60::U!{i32}::U, i32}::foo (u);
    <unit-value>
}
