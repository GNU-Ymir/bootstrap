// A union declaring a base dispatches the public methods of that base.

class Base {
    pub self () {}
    pub fn foo (self)-> i32 { 0 }
}

class A over Base {
    pub self () {}
}

class B over Base {
    pub self () {}
}

union U over Base
| A
| B;

fn bar (u : &U)-> i32 {
    u.foo ()
}
