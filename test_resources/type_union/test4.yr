// A union over class members, all descending from the declared base.

class Base {
    pub self () {}
}

class A over Base {
    pub self () {}
}

class B over Base {
    pub self () {}
}

union U over Base
| A
| B;

fn foo (u : &U) {
    u;
}
