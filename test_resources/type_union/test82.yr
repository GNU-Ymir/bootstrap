// A field assign method is mutable, so the union it is reached on has to be.

record A {
    let mut _i : i32 = 0;
    pub self () {}

    @field
    pub fn foo (mut self, i : i32) { self._i = i; }
}

union U
| A;

fn main () {
    let x : U = A ();
    x.foo = 89;
}
