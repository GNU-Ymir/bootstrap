// A union declaring a base implements only what the base implements, even when
// every member implements more.

trait Show {
    pub fn show (self)-> i32;
}

class Base {
    pub self () {}
}

class A over Base {
    pub self () {}
    impl Show {
        pub over show (self)-> i32 { 1 }
    }
}

class B over Base {
    pub self () {}
    impl Show {
        pub over show (self)-> i32 { 2 }
    }
}

union U over Base
| A
| B;

fn foo {T impl Show} (a : T)-> i32 {
    a.show ()
}

fn bar (u : &U)-> i32 {
    foo (u)
}
