frame: test72::A::foo (mut self: mut &(mut test72::A), i: i32)-> void {
    self._i = i;
    <unit-value>
}
frame: test72::A::self (mut self: mut &(mut test72::A), next: (dg ()-> void)[nullable])-> void {
    {
        self._i = 0;
        if (next.func != null) (next.func)(next.closure)
    };
    <unit-value>
}
frame: test72::B::foo (mut self: mut &(mut test72::B), i: i32)-> void {
    self._i = (i + 1);
    <unit-value>
}
frame: test72::B::self (mut self: mut &(mut test72::B), next: (dg ()-> void)[nullable])-> void {
    {
        self._i = 0;
        if (next.func != null) (next.func)(next.closure)
    };
    <unit-value>
}
frame: test72::bar (mut u: mut &(mut test72::U))-> void {
    test72::U::foo(u:, 89);
    <unit-value>
}
