// A deeply mutable union borrows the member it holds, so the member is aliased
// explicitly.

class A {
    pub self () {}
}

class B {
    pub self () {}
}

union U
| A
| B;

fn take (dmut u : &U) {
    u;
}

fn main () {
    let dmut a = A ();
    take (a);
}
