mod test1;

def foo (a : f32) {
     a;
}

def main () {
    let mut a = 1.2f;
    let b : f80 = cast!f80 (a);
    let c : f32 = cast!f32 (b);
    let d : f64 = cast!f64 (b);
    let e : f64 = cast!f64 (c);

    (b, " ", sizeof (typeof (b)));
    (c, " ", sizeof (typeof (c)));
    (d, " ", sizeof (typeof (d)));
    (e, " ", sizeof (typeof (e)));

    foo (a);
}
