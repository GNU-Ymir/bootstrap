/*
 * A compile time constant is folded at its use site and referenced by no YIL symbol,
 * so the module declaring it only appears in the dependencies of its readers if the
 * reads made during validation are recorded.
 */

in test1;

mod ::codes;
mod ::reader;
mod ::silent;
