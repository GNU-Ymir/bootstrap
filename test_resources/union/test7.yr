union A {
    pub let a : i32;
    pub let b : i64 = 0;
    pub let c : (i64, i64) = (0, 0);
    pub let d : (i64, i64, i64) = (0, 0, 0);

    pub self () with a = 8 {}
}

fn main () {
    let _fields2_ = A::field_infos;
    let _size_ = A::size;
}
