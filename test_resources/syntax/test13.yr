/**
 * The map comprehension `[k => v for i in it]`. The construct parses, but the
 * validator does not implement it yet -- the point of the case is the syntax
 * tree it produces.
 * */

in test13;

fn main () {
    let src = [1, 2, 3, 4];
    let _mfr_ = copy [x => x * 2 for x in src];
}
