in test18;
fn params ()-> [(i32, [c8])] {
    copy [(3, "foo"), (2, "bar")]
}

@{parameters(params ())}
__test named (len: i32, text: [c8]) {
    assert (text.len == cast !{usize}(len));
}

@{parameters(copy [1, 2, 3])}
__test (value: i32) {
    assert (value > 0);
}

