in test18;
yield params ()-> (i32, [c8]) {
    yield (3, "foo");
    yield (2, "bar")
}

@{parameters(params ())}
__test named (len: i32, text: [c8]) {
    assert (text.len == cast !{usize}(len));
}

@{parameters(( yield i for i in 1 ... 3))}
__test (value: i32) {
    assert (value > 0);
}

