in test20;
yield rows (n: i32)-> dmut [i32] {
    for i in 0 .. n {
        yield copy [i]
    }
}

yield all (n: i32)-> dmut [i32] {
    let dmut g = rows (n);
    yield in alias g;
    yield copy [n]
}

fn main () {
    for x in all (2) {
        let _ = x;
    }
}

