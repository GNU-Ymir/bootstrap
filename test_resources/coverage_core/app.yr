in app;

use core;

/**
 * A module of the package that is not core, instrumented as usual
 */
pub fn appFn (i: i32)-> i32 {
    if (i > 0) {
        return core::coreFn (core::coreTemplate!{i32} (i, i));
    }

    return 0;
}
