// A field of another enum tests a value of ours without naming the field
// carrying it, so it covers none.

enum X
| A = 1
| B = 2;

enum Y
| A = 2;

fn get (x : X)-> i32 {
    match x {
        X::A => { 1 }
        Y::A => { 2 }
    }
}

fn main () {
    get (X::A);
}
