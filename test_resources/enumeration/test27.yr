// An arm naming several fields covers each of them.

enum X
| A = 1
| B = 2
| C = 3;

fn get (x : X)-> i32 {
    match x {
        X::A | X::B => { 1 }
        X::C => { 3 }
    }
}

fn main () {
    get (X::B);
}
