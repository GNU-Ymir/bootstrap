// A match over an enum must be complete even when its arms produce nothing, so
// the field no arm names is reported.

enum X
| A = 1
| B = 2
| C = 3;

fn run (x : X) {
    match x {
        X::A => {}
        X::C => {}
    }
}

fn main () {
    run (X::A);
}
