// Two fields sharing a value are two fields, each named by its own arm, so a
// match naming one of them and not the other is not complete.

enum X
| A = 1
| B = 1
| C = 2;

fn get (x : X)-> i32 {
    match x {
        X::A => { 1 }
        X::C => { 2 }
    }
}

fn main () {
    get (X::B);
}
