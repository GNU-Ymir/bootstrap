// A default arm completes a match over an enum leaving fields out.

enum X
| A = 1
| B = 2
| C = 3;

fn run (x : X) {
    match x {
        X::A => {}
        _ => {}
    }
}

fn main () {
    run (X::A);
}
