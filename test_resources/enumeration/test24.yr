// A match leaving a field out is not complete, so its arms cannot produce a
// value, and the field no arm names is reported.

enum X
| A = 1
| B = 2
| C = 3;

fn get (x : X)-> i32 {
    match x {
        X::A => { 1 }
        X::C => { 3 }
    }
}

fn main () {
    get (X::A);
}
