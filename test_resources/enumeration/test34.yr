// A match over an enum naming every field is complete, whatever its arms
// produce.

enum X
| A = 1
| B = 2
| C = 3;

fn run (x : X) {
    match x {
        X::A => {}
        X::B => {}
        X::C => {}
    }
}

fn main () {
    run (X::A);
}
