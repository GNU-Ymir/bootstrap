// A guard decides the arm too, so an arm carrying one does not cover the field
// it names.

enum X
| A = 1
| B = 2;

fn get (x : X, b : bool)-> i32 {
    match x {
        X::A if b => { 1 }
        X::B => { 2 }
    }
}

fn main () {
    get (X::A, true);
}
