// The fields of an enum sharing a value are covered by naming each of them.

enum X
| A = 1
| B = 1
| C = 2;

fn get (x : X)-> i32 {
    match x {
        X::A => { 1 }
        X::B => { 2 }
        X::C => { 3 }
    }
}

fn main () {
    get (X::B);
}
