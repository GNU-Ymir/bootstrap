// A default arm added to a match already covering every field is never
// evaluated.

enum X
| A = 1
| B = 2;

fn main () {
    let x = X::A;
    match x {
        X::A => {}
        X::B => {}
        _ => {}
    }
}
