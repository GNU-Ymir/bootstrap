// the yielded type of an immutable generator handle cannot be mutable
yield rows (n: i32)-> dmut [i32] {
    for i in 0 .. n {
        yield copy [i, i];
    }
}

fn main () {
    let g: yield-> dmut [i32] = rows (3);
    g;
}
