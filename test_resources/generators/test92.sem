frame: impl::each (ref self: test92::R)-> (yield-> i32) {
    yield self.n
}
frame: test92::R::self (mut ref self: mut test92::R, n: i32)-> void {
    self.n = n;
    <unit-value>
}
frame: test92::main ()-> void {
    {
        let #_iter: (yield-> i32) = impl::each(test92::R::self(new, 1));
        let mut #_yielded: mut i32;
        while (#_iter).next(#_yielded) {
            let i: i32 = #_yielded;
            {
                let _: i32 = i;
                <unit-value>
            }
        }
    }
}
