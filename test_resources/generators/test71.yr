// a deep copy of a slice holding generator comprehensions copies the slice, not the generators
fn main () {
    let g = dcopy [(yield x for x in 0 .. 3) for _ in 0 .. 2];
    for v in g[0] {
        let _: i32 = v;
    }
}
