// the index alias operator is not defined on a generator
yield rows (n: i32)-> dmut [i32] {
    for i in 0 .. n {
        yield copy [i, i];
    }
}

fn main () {
    let dmut g = rows (3);
    g:[0];
}
