frame: test79::A::each (self: &(test79::A))-> (yield-> i32) {
    {
        let mut #_iter: usize = 0us;
        let #_nb_iters: usize = if (0 > self.n) 0 else self.n;
        while (#_iter < #_nb_iters) {
            let mut i: i32 = 0;
            i = cast!{i32}(#_iter);
            {
                yield i
            }
        } step #_iter = (#_iter + 1us)
    }
}
frame: test79::A::self (mut self: mut &(mut test79::A), n: i32, next: (dg ()-> void)[nullable])-> void {
    {
        self.n = n;
        if (next.func != null) (next.func)(next.closure)
    };
    <unit-value>
}
frame: test79::sum (a: &(test79::A))-> i32 return {
    let mut s: mut i32 = 0;
    {
        let #_iter: (yield-> i32) = test79::A::each(a);
        let mut #_yielded: mut i32;
        while (#_iter).next(#_yielded) {
            let i: i32 = #_yielded;
            {
                s = (s + i);
                <unit-value>
            }
        }
    };
    s
}
frame: test79::main ()-> void {
    let _: i32 = test79::sum (test79::A::self(new, 3));
    <unit-value>
}
