frame: test30::main ()-> void {
    let p: (yield-> i32) = test30::pair ();
    let n: (i32)? = {
            let mut #_yielded: mut i32;
            if (p).next(#_yielded) (#_yielded)? else none
        };
    if !if n.hasValue (n.value == 1) else false throw core::exception::assertion::AssertError::self(new, ""s8);
    <unit-value>
}
frame: test30::pair ()-> (yield-> i32) {
    yield 1;
    yield 2
}
