// a generator comprehension collected back into a slice
fn main ()
    throws AssertError
{
    let src = copy [1, 2, 3];
    let g = (yield x * 2 for x in src);
    let s = copy [i for i in g];
    assert (s == [2, 4, 6]);
}
