// a generator method cannot throw, nor take a parameter by reference
class A {
    pub self () {}

    pub yield f (self)-> i32
        throws AssertError
    {
        yield 1;
    }

    pub yield g (self, ref x: i32)-> i32 {
        yield x;
    }
}

fn main () {
    let _ = A ();
}
