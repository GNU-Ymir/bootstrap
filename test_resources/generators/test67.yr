// the element of a generator comprehension can break its own loops
fn main () {
    let g = (yield {
        let mut n = 0;
        loop {
            if (n >= x) break;
            n += 1;
        }
        n
    } for x in 0 .. 3);

    for v in g {
        let _: i32 = v;
    }
}
