// alias of the handle of a generator whose values are not borrowable
yield rows (n: i32)-> [i32] {
    for i in 0 .. n {
        yield copy [i, i];
    }
}

fn main () {
    let dmut g = rows (3);
    for x in alias g {
        x;
    }
}
