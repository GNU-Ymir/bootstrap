frame: [weak] core::types::array::opEquals!{[i32], i32}::opEquals (a: [i32], b: [i32])-> bool return {
    if (a.len != b.len) {
        return false;
    };
    {
        let mut #_iter: usize = 0us;
        let #_nb_iters: usize = a.len;
        while (#_iter < #_nb_iters) {
            let mut i: usize = 0us;
            i = #_iter;
            {
                if (#{
                    if (a.len <= i) panic;
                    a [i]
                #} != #{
                    if (b.len <= i) panic;
                    b [i]
                #}) return false
            }
        } step #_iter = (#_iter + 1us)
    };
    true
}
frame: [weak] test72::main::_5_13 (#_iterated: (..i32))-> (yield-> mut i32) {
    let mut #_iter: usize = 0us;
    let #_nb_iters: usize = {
            let #_range: (i32, i32, i32) = (#_iterated.fst, #_iterated.scd, #_iterated.step);
            let #_val: (i32, i32, i32, i32) = if (#_range.2 < 0) (-#_range.2, #_range.1, #_range.0, (#_range.0 - #_range.1)) else (#_range.2, #_range.0, #_range.1, (#_range.1 - #_range.0));
            let #_nbAll: i32 = (#_val.3 / #_val.0);
            if (#_val.1 > #_val.2) 0 else if (#_nbAll == 18446744073709551615) #_nbAll else if ((((#_nbAll * #_val.0) + #_val.1) != #_val.2) || #_iterated.contains) (#_nbAll + 1) else #_nbAll
        };
    let #_fst: i32 = #_iterated.fst;
    let #_step: i32 = #_iterated.step;
    while (#_iter < #_nb_iters) {
        let mut x: i32 = 0;
        x = (#_fst + cast!{i32}((#_iter * cast!{usize}(#_step))));
        if (x != 2) yield x
    } step #_iter = (#_iter + 1us)
}
frame: test72::main ()-> void {
    let g: (yield-> i32) = test72::main::_5_13 (#_iterated: (..i32))-> (yield-> mut i32) ((0 ..[false] 10).step_by(1));
    if !core::types::array::opEquals!{[i32], i32}::opEquals ({
            let #_iter: (yield-> i32) = g;
            let mut #_yielded: mut i32;
            let mut #_value: mut [mut i32] = copy [<unit-value> ; 0us];
            while (#_iter).next(#_yielded) {
                let v: i32 = #_yielded;
                #_value ~= {.len = 1us, .ptr = &([v])}
            };
            #_value
        }, {.len = 9us, .ptr = &([0, 1, 3, 4, 5, 6, 7, 8, 9])}) throw core::exception::assertion::AssertError::self(new, ""s8);
    <unit-value>
}
