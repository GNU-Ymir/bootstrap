// a generator comprehension capturing a local, returned by the function creating it
fn make (k: i32)-> (yield-> i32) {
    let m = k + 1;
    return (yield x * m for x in 0 .. 3);
}

fn main () {
    for x in make (2) {
        let _: i32 = x;
    }
}
