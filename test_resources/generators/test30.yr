// the fake method next, resuming a generator into an option
yield pair ()-> i32 {
    yield 1;
    yield 2;
}

fn main ()
    throws AssertError
{
    let p = pair ();
    let n: i32? = p.next ();

    assert (n == 1?);
}
