// an element catching what it throws into an option does not throw
fn check (x: i32)-> i32
    throws AssertError
{
    assert (x > 0);
    x
}

fn main () {
    for v in (yield check (x)? for x in 0 .. 3) {
        let _: (i32)? = v;
    }
}
