frame: test21::gen (s: [i32])-> (yield-> i32) {
    {
        let #_value: [i32] = s;
        let mut #_index: usize = 0us;
        while (#_index < #_value.len) {
            let x: i32 = #_value [#_index];
            {
                yield x
            }
        } step #_index = (#_index + 1us)
    }
}
frame: test21::main ()-> void {
    let g: (yield-> i32) = test21::make ();
    {
        let #_iter: (yield-> i32) = g;
        let mut #_yielded: mut i32;
        while (#_iter).next(#_yielded) {
            let i: i32 = #_yielded;
            {
                let _: i32 = i;
                break ;
            }
        }
    };
    {
        let #_iter: (yield-> i32) = g;
        let mut #_yielded: mut i32;
        while (#_iter).next(#_yielded) {
            let z: i32 = #_yielded;
            {
                let _: i32 = z;
                break ;
            }
        }
    }
}
frame: test21::make ()-> (yield-> i32) {
    let arr: [i32] = copy ({.len = 3us, .ptr = &([1, 2, 3])});
    return test21::gen (arr);
}
