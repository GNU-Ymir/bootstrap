frame: test13::fib (n: i32)-> (yield-> u64) {
    #{
        let mut a: mut u64 = 0u64;
        let mut b: mut u64 = 1u64
    #};
    {
        let mut #_iter: usize = 0us;
        let #_nb_iters: usize = {
                let #_range: (i32, i32, i32) = (0, n, if (0 <= n) 1 else -1);
                let #_val: (i32, i32, i32, i32) = if (#_range.2 < 0) (-#_range.2, #_range.1, #_range.0, (#_range.0 - #_range.1)) else (#_range.2, #_range.0, #_range.1, (#_range.1 - #_range.0));
                let #_nbAll: i32 = (#_val.3 / #_val.0);
                if (#_nbAll == 18446744073709551615) #_nbAll else if (((#_nbAll * #_val.0) + #_val.1) != #_val.2) (#_nbAll + 1) else #_nbAll
            };
        let #_step: i32 = if (0 <= n) 1 else -1;
        while (#_iter < #_nb_iters) {
            {
                yield a;
                let c: u64 = (a + b);
                a = b;
                b = c;
                <unit-value>
            }
        } step #_iter = (#_iter + 1us)
    }
}
frame: [weak] test13::main::_29_15 (x: u64)-> mut u64 return (x + 1u64)
frame: test13::main ()-> void {
    let f: [u64] = {
            let #_iter: (yield-> u64) = test13::fib (10);
            let mut #_yielded: mut u64;
            let mut #_value: mut [mut u64] = copy [<unit-value> ; 0us];
            while (#_iter).next(#_yielded) {
                let x: u64 = #_yielded;
                #_value ~= {.len = 1us, .ptr = &([x])}
            };
            #_value
        };
    let e: [i32] = {
            let #_iter: (yield-> i32) = test13::evens (copy ({.len = 4us, .ptr = &([1, 2, 3, 4])}));
            let mut #_yielded: mut i32;
            let mut #_value: mut [mut i32] = copy [<unit-value> ; 0us];
            while (#_iter).next(#_yielded) {
                let x: i32 = #_yielded;
                #_value ~= {.len = 1us, .ptr = &([x])}
            };
            #_value
        };
    let add: λ (_)-> _ = |x|-> _;
    {
        let #_iter: (yield-> u64) = test13::fib (3);
        let mut #_yielded: mut u64;
        while (#_iter).next(#_yielded) {
            let i: u64 = #_yielded;
            {
                (x: u64)-> mut u64 (i);
                <unit-value>
            }
        }
    };
    if !((f.len == 10us) && (e.len == 2us)) throw core::exception::assertion::AssertError::self(new, ""s8);
    <unit-value>
}
frame: test13::evens (src: [i32])-> (yield-> i32) {
    {
        let #_value: [i32] = src;
        let mut #_index: usize = 0us;
        while (#_index < #_value.len) {
            let x: i32 = #_value [#_index];
            {
                if ((x % 2) == 0) {
                    let doubled: i32 = (x * 2);
                    yield (doubled / 2)
                } else {
                    let doubled: i32 = x;
                    <unit-value>
                }
            }
        } step #_index = (#_index + 1us)
    }
}
