// the frame of a record generator holds a copy of self, which cannot be mutable
record R {
    pub let mut n: i32;

    pub self (n: i32) with n = n {}

    pub yield each (mut self)-> i32 {
        self.n += 1;
        yield self.n;
    }
}

fn main () {
    let _ = R (1);
}
