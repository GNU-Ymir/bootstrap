// an immutable copy of a generator handle does not yield mutable values
yield rows (n: i32)-> dmut [i32] {
    for i in 0 .. n {
        yield copy [i, i];
    }
}

fn main () {
    let dmut g = rows (3);
    let g2 = g;
    for dmut x in g2 {
        x[0] = 1;
    }
}
