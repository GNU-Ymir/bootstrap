// a filtered generator comprehension yields nothing for a rejected iteration
fn main ()
    throws AssertError
{
    let g = (yield x for x in 0 .. 10 if x != 2);
    assert (copy [v for v in g] == [0, 1, 3, 4, 5, 6, 7, 8, 9]);
}
