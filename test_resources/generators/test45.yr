// yield in is a yield, it must be inside a generator
yield count (n: i32)-> i32 {
    for i in 0 .. n {
        yield i;
    }
}

fn all (n: i32) {
    yield in count (n);
}

fn main () {
    all (2);
}
