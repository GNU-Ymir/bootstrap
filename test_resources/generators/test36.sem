frame: test36::main ()-> void {
    let mut g: mut (yield-> mut [mut i32]) = test36::rows (3);
    let mut n: mut (mut [mut i32])? = {
            let mut #_yielded: mut [i32];
            if (alias (g)).next(#_yielded) (alias (#_yielded))? else none
        };
    if (n.hasValue && #{
        let mut x: mut [mut i32] = alias (n.value);
        true
    #}) {
        #{
            if (x.len <= 0us) panic;
            x [0us]
        #} = 12;
        if !(#{
            if (x.len <= 0us) panic;
            x [0us]
        #} == 12) throw core::exception::assertion::AssertError::self(new, ""s8);
        <unit-value>
    }
}
frame: test36::rows (n: i32)-> (yield-> mut [mut i32]) {
    {
        let mut #_iter: usize = 0us;
        let #_nb_iters: usize = if (0 > n) 0 else n;
        while (#_iter < #_nb_iters) {
            let mut i: i32 = 0;
            i = cast!{i32}(#_iter);
            {
                yield copy ({.len = 2us, .ptr = &([i, i])})
            }
        } step #_iter = (#_iter + 1us)
    }
}
