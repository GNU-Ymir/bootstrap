// the handle of a generator yielding borrowable data is copied mutably without alias
yield rows (n: i32)-> dmut [i32] {
    for i in 0 .. n {
        yield copy [i, i];
    }
}

fn main () {
    let dmut g = rows (3);
    let dmut g2 = g;
    g2;
}
