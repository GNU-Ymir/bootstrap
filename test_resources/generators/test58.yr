// a generator comprehension cannot capture a value with a destructor
entity Handle {
    pub let x: i32 = 1;
    pub self () {}
    __dtor (mut self) {}
}

fn main () {
    let h = Handle ();
    let _ = (yield h.x + x for x in 0 .. 3);
}
