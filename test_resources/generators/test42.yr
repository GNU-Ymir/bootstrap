// yielding the mutable borrowed values of a generator needs an alias
yield rows (n: i32)-> dmut [i32] {
    for i in 0 .. n {
        yield copy [i, i];
    }
}

yield all (n: i32)-> dmut [i32] {
    let dmut g = rows (n);
    yield in g;
}

fn main () {
    for x in all (3) {
        x;
    }
}
