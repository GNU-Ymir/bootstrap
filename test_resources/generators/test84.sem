frame: test84::A::self (mut self: mut &(mut test84::A), next: (dg ()-> void)[nullable])-> void {
    if (next.func != null) (next.func)(next.closure);
    <unit-value>
}
frame: test84::B::self::_15_9 (#{CLOSURE-VARREF}: *((mut &(mut test84::B), (dg ()-> void)[nullable])))-> void {
    if (next.func != null) (next.func)(next.closure);
    <unit-value>
}
frame: test84::B::self (mut self: mut &(mut test84::B), next: (dg ()-> void)[nullable])-> void {
    test84::A::self(self, {&((self, next));(#{CLOSURE-VARREF}: *((mut &(mut test84::B), (dg ()-> void)[nullable])))-> void});
    <unit-value>
}
frame: [weak] test84::Seq!{i32}::Seq::each (self: &(test84::A), x: i32)-> (yield-> i32) {
    yield x
}
frame: test84::main ()-> void {
    {
        let #_iter: (yield-> i32) = test84::Seq!{i32}::Seq::each(test84::B::self(new), 1);
        let mut #_yielded: mut i32;
        while (#_iter).next(#_yielded) {
            let i: i32 = #_yielded;
            {
                let _: i32 = i;
                <unit-value>
            }
        }
    }
}
