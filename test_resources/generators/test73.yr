// the filter of a generator comprehension captures what it uses, as the element does
fn main ()
    throws AssertError
{
    let h = 3;
    let g = (yield x + h for x in 0 .. 6 if x != h);
    assert (copy [v for v in g] == [3, 4, 5, 7, 8]);
}
