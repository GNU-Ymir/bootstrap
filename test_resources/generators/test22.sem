frame: test22::main ()-> void {
    let mut arr: mut [mut i32] = copy ({.len = 2us, .ptr = &([1, 2])});
    let mut g: mut (yield-> mut [mut i32]) = test22::rows (alias (arr));
    {
        let #_iter: (yield-> mut [mut i32]) = g;
        let mut #_yielded: mut [i32];
        while (#_iter).next(#_yielded) {
            let mut x: mut [mut i32] = alias (#_yielded);
            {
                #{
                    if (x.len <= 0us) panic;
                    x [0us]
                #} = 12;
                <unit-value>
            }
        }
    };
    if !(#{
        if (arr.len <= 0us) panic;
        arr [0us]
    #} == 12) throw core::exception::assertion::AssertError::self(new, ""s8);
    <unit-value>
}
frame: test22::rows (mut a: mut [mut i32])-> (yield-> mut [mut i32]) {
    yield alias (a)
}
