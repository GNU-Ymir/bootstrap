// a generator whose element type does not match the specialization is rejected
yield src ()-> i32 {
    yield 1;
}

fn collect {T of (yield-> f64)} (g: T)-> [f64] {
    copy [x for x in g]
}

fn main () {
    let _ = collect (src ());
}
