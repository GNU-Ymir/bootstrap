frame: test50::main ()-> void {
    {
        let #_iter: (yield-> i32) = test50::make (2);
        let mut #_yielded: mut i32;
        while (#_iter).next(#_yielded) {
            let x: i32 = #_yielded;
            {
                let _: i32 = x;
                <unit-value>
            }
        }
    }
}
frame: [weak] test50::make::_4_12 (#{CLOSURE-VARREF}: (i32,), #_iterated: (..i32))-> (yield-> mut i32) {
    let mut #_iter: usize = 0us;
    let #_nb_iters: usize = {
            let #_range: (i32, i32, i32) = (#_iterated.fst, #_iterated.scd, #_iterated.step);
            let #_val: (i32, i32, i32, i32) = if (#_range.2 < 0) (-#_range.2, #_range.1, #_range.0, (#_range.0 - #_range.1)) else (#_range.2, #_range.0, #_range.1, (#_range.1 - #_range.0));
            let #_nbAll: i32 = (#_val.3 / #_val.0);
            if (#_nbAll == 18446744073709551615) #_nbAll else if ((((#_nbAll * #_val.0) + #_val.1) != #_val.2) || #_iterated.contains) (#_nbAll + 1) else #_nbAll
        };
    let #_fst: i32 = #_iterated.fst;
    let #_step: i32 = #_iterated.step;
    while (#_iter < #_nb_iters) {
        let mut x: i32 = 0;
        x = (#_fst + cast!{i32}((#_iter * cast!{usize}(#_step))));
        yield (x * m)
    } step #_iter = (#_iter + 1us)
}
frame: test50::make (k: i32)-> (yield-> i32) {
    let m: i32 = (k + 1);
    return test50::make::_4_12 (#{CLOSURE-VARREF}: (i32,), #_iterated: (..i32))-> (yield-> mut i32) ((m,), (0 ..[false] 3).step_by(1));
}
