frame: test82::A::self (mut self: mut &(mut test82::A), next: (dg ()-> void)[nullable])-> void {
    if (next.func != null) (next.func)(next.closure);
    <unit-value>
}
frame: [weak] test82::A::repeat!{[c8]}::repeat (self: &(test82::A), x: [c8], n: i32)-> (yield-> [c8]) {
    {
        let mut #_iter: usize = 0us;
        let #_nb_iters: usize = if (0 > n) 0 else n;
        while (#_iter < #_nb_iters) {
            {
                yield x
            }
        } step #_iter = (#_iter + 1us)
    }
}
frame: [weak] test82::A::repeat!{i32}::repeat (self: &(test82::A), x: i32, n: i32)-> (yield-> i32) {
    {
        let mut #_iter: usize = 0us;
        let #_nb_iters: usize = if (0 > n) 0 else n;
        while (#_iter < #_nb_iters) {
            {
                yield x
            }
        } step #_iter = (#_iter + 1us)
    }
}
frame: test82::main ()-> void {
    let a: &(test82::A) = test82::A::self(new);
    {
        let #_iter: (yield-> i32) = test82::A::repeat!{i32}::repeat(a, 1, 3);
        let mut #_yielded: mut i32;
        while (#_iter).next(#_yielded) {
            let i: i32 = #_yielded;
            {
                let _: i32 = i;
                <unit-value>
            }
        }
    };
    {
        let #_iter: (yield-> [c8]) = test82::A::repeat!{[c8]}::repeat(a, "s"s8, 2);
        let mut #_yielded: mut [c8];
        while (#_iter).next(#_yielded) {
            let s: [c8] = #_yielded;
            {
                let _: [c8] = s;
                <unit-value>
            }
        }
    }
}
