// the generator of the roadmap, consumed by a list comprehension
yield foo (n: i32)-> i32 {
    for i in 0 .. n {
        yield i;
    }
}

fn main ()
    throws AssertError
{
    let x = foo (12);
    let lst = copy [i for i in x];
    assert (lst == [i for i in 0 .. 12]);
}
