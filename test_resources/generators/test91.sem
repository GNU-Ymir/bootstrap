frame: test91::R::each (ref self: test91::R)-> (yield-> i32) {
    {
        let mut #_iter: usize = 0us;
        let #_nb_iters: usize = if (0 > self.n) 0 else self.n;
        while (#_iter < #_nb_iters) {
            let mut i: i32 = 0;
            i = cast!{i32}(#_iter);
            {
                yield i
            }
        } step #_iter = (#_iter + 1us)
    }
}
frame: test91::R::self (mut ref self: mut test91::R, n: i32)-> void {
    self.n = n;
    <unit-value>
}
frame: test91::main ()-> void {
    {
        let #_iter: (yield-> i32) = test91::make ();
        let mut #_yielded: mut i32;
        while (#_iter).next(#_yielded) {
            let i: i32 = #_yielded;
            {
                let _: i32 = i;
                <unit-value>
            }
        }
    }
}
frame: test91::make ()-> (yield-> i32) return {
    let r: test91::R = test91::R::self(new, 3);
    test91::R::each(r)
}
