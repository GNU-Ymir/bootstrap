frame: test80::A::each (self: &(test80::A))-> (yield-> i32) {
    yield 1
}
frame: test80::A::self (mut self: mut &(mut test80::A), next: (dg ()-> void)[nullable])-> void {
    if (next.func != null) (next.func)(next.closure);
    <unit-value>
}
frame: test80::B::each (self: &(test80::B))-> (yield-> i32) {
    yield 2;
    yield 3
}
frame: test80::B::self::_11_9 (#{CLOSURE-VARREF}: *((mut &(mut test80::B), (dg ()-> void)[nullable])))-> void {
    if (next.func != null) (next.func)(next.closure);
    <unit-value>
}
frame: test80::B::self (mut self: mut &(mut test80::B), next: (dg ()-> void)[nullable])-> void {
    test80::A::self(self, {&((self, next));(#{CLOSURE-VARREF}: *((mut &(mut test80::B), (dg ()-> void)[nullable])))-> void});
    <unit-value>
}
frame: test80::sum (a: &(test80::A))-> i32 return {
    let mut s: mut i32 = 0;
    {
        let #_iter: (yield-> i32) = test80::A::each(a);
        let mut #_yielded: mut i32;
        while (#_iter).next(#_yielded) {
            let i: i32 = #_yielded;
            {
                s = (s + i);
                <unit-value>
            }
        }
    };
    s
}
frame: test80::main ()-> void {
    let _: i32 = test80::sum (test80::B::self(new));
    <unit-value>
}
