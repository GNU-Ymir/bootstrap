// a generator of generators, the bound of the inner ones captured by the outer one
fn main () {
    let k = 3;
    let g = (yield (yield x + y for y in 0 .. k) for x in 0 .. 3);
    for inner in g {
        for y in inner {
            let _: i32 = y;
        }
    }
}
