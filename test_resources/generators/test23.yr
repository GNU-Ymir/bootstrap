// a list comprehension borrowing mutably the values of a generator, by alias
yield rows (n: i32)-> dmut [i32] {
    for i in 0 .. n {
        yield copy [i, i];
    }
}

fn main () {
    let dmut g = rows (3);
    let l = copy [x for dmut x in alias g];
    l;
}
