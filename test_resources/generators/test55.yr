// a generator comprehension in a template, yielding its parameter type
fn each {T} (a: [T])-> (yield-> T) {
    (yield x for x in a)
}

fn main () {
    for x in each (copy [1, 2]) {
        let _: i32 = x;
    }

    for x in each (copy ["a"s8]) {
        let _: [c8] = x;
    }
}
