// a generator passed as a yield-> T parameter
yield count (n: i32)-> i32 {
    for i in 0 .. n {
        yield i;
    }
}

fn sum (g: yield-> i32)-> i32 {
    let mut s = 0;
    for x in g {
        s += x * 2;
    }
    s
}

fn main () {
    let _ = sum (count (4));
}
