// the element type of a generator parameter is bound by template specialization
yield src ()-> i32 {
    yield 1;
    yield 2;
}

fn collect {U} (g: yield-> U)-> [U] {
    copy [x for x in g]
}

fn main ()
    throws AssertError
{
    let r: [i32] = collect (src ());
    assert (r == [1, 2]);
}
