frame: [weak] std::conv::to!{[c8], c8, 10, i32, i32}::to (a: i32, upper: bool = false)-> mut [mut c8] {
    if (a == 0) {
        return copy ("0"s8);
    };
    #{
        let mut n: mut i32 = if (a < 0) (-a, 0us, true) else (a, 0us, false).0;
        let mut len: mut usize = if (a < 0) (-a, 0us, true) else (a, 0us, false).1;
        let sign: bool = if (a < 0) (-a, 0us, true) else (a, 0us, false).2
    #};
    let absVal: i32 = n;
    let b: i32 = 10;
    while (n > 0) {
        len = (len + 1us);
        n = (n / b);
        <unit-value>
    };
    if !sign {
        let mut res: mut [mut c8] = core::types::array::allocArray!{c8}::allocArray (len);
        let mut m: mut i32 = absVal;
        {
            let mut #_iter: usize = 0us;
            let #_nb_iters: usize = {
                    let #_range: (mut usize, usize, isize) = (len, 0us, if (len <= 0us) 1is else -1is);
                    let #_val: (mut usize, mut usize, mut usize, mut usize) = if (#_range.2 < 0is) (-#_range.2, #_range.1, #_range.0, (#_range.0 - #_range.1)) else (#_range.2, #_range.0, #_range.1, (#_range.1 - #_range.0));
                    let #_nbAll: mut usize = (#_val.3 / #_val.0);
                    if (#_nbAll == 18446744073709551615us) #_nbAll else if (((#_nbAll * #_val.0) + #_val.1) != #_val.2) (#_nbAll + 1us) else #_nbAll
                };
            while (#_iter < #_nb_iters) {
                let mut i: usize = 0us;
                i = (len + (#_iter * cast!{usize}(if (len <= 0us) 1is else -1is)));
                {
                    #{
                        if (res.len <= (i - 1us)) panic;
                        res [(i - 1us)]
                    #} = cast!{c8}((48u8 + cast!{u8}((m % b))));
                    m = (m / b);
                    <unit-value>
                }
            } step #_iter = (#_iter + 1us)
        };
        return alias (res);
    } else {
        let mut res: mut [mut c8] = core::types::array::allocArray!{c8}::allocArray ((len + 1us));
        #{
            if (res.len <= 0us) panic;
            res [0us]
        #} = '-'c8;
        let mut m: mut i32 = absVal;
        {
            let mut #_iter: usize = 0us;
            let #_nb_iters: usize = {
                    let #_range: (mut usize, usize, isize) = (len, 0us, if (len <= 0us) 1is else -1is);
                    let #_val: (mut usize, mut usize, mut usize, mut usize) = if (#_range.2 < 0is) (-#_range.2, #_range.1, #_range.0, (#_range.0 - #_range.1)) else (#_range.2, #_range.0, #_range.1, (#_range.1 - #_range.0));
                    let #_nbAll: mut usize = (#_val.3 / #_val.0);
                    if (#_nbAll == 18446744073709551615us) #_nbAll else if (((#_nbAll * #_val.0) + #_val.1) != #_val.2) (#_nbAll + 1us) else #_nbAll
                };
            while (#_iter < #_nb_iters) {
                let mut i: usize = 0us;
                i = (len + (#_iter * cast!{usize}(if (len <= 0us) 1is else -1is)));
                {
                    #{
                        if (res.len <= i) panic;
                        res [i]
                    #} = cast!{c8}((48u8 + cast!{u8}((m % b))));
                    m = (m / b);
                    <unit-value>
                }
            } step #_iter = (#_iter + 1us)
        };
        return alias (res);
    };
}
frame: [weak] std::conv::to!{[c8], c8, i32}::to (a: i32)-> mut [mut c8] return {
    alias (std::conv::to!{[c8], c8, 10, i32, i32}::to (a, false))
}
frame: [weak] core::types::array::allocArray!{c8}::allocArray (len: usize)-> mut [mut c8] return {
    let mut res: mut [mut c8] = {.len = 0us, .ptr = null};
    core::types::array::YRUNTIME!{c8}::YRUNTIME::_yrt_alloc_slice_no_set (ref (res), len, 1us);
    alias (res)
}
frame: [weak] test7::main::_7_28 (a: i32)-> i32 return (a + 1)
frame: test7::main ()-> void {
    let x: i32 = 10;
    let str: [c8] = "result: "s8 ~ std::conv::to!{[c8], c8, i32}::to ((a: i32)-> i32 (x));
    if !core::types::array::opEquals (str, "result: 11"s8) throw core::exception::assertion::AssertError::self(new, ""s8);
    <unit-value>
}
