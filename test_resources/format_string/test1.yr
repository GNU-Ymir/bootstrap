in test1;

record Foo {
    pub let x: i32;

    pub self (x: i32)
        with x = x
    {}
}

fn main ()
    throws (core::exception::assertion::AssertError)
{
    let x = 12;
    let y = Foo (12);
    let str = f"${x}, ${y}, ${x + y.x}";

    assert (str == "12, Foo(12), 24");
}
