frame: [weak] std::conv::to!{[c8], c8, test1::Foo}::to (ref a: test1::Foo)-> mut [mut c8] {
    let mut stream: mut &(mut std::stream::StringStream) = std::stream::StringStream::self(new);
    std::stream::StringStream::write!{test1::Foo}::write(stream:, ref (a));
    return std::stream::StringStream::opIndex(stream:);
}
frame: [weak] std::conv::to!{[c8], c8, 10, i32, i32}::to (a: i32, mut ref res: mut [mut c8], upper: bool = false)-> void {
    let b: i32 = 10;
    let sign: bool = (a < 0);
    let negVal: i32 = if sign a else -a;
    let mut n: mut i32 = negVal;
    let mut len: mut usize = 1us;
    while (n <= -10) {
        len = (len + 1us);
        n = (n / b);
        <unit-value>
    };
    let mut region: mut [mut c8] = {.len = 0us, .ptr = null};
    let mut m: mut i32 = negVal;
    if sign {
        std::conv::utils::Grow!{c8}::Grow::_yrt_grow_slice (ref (res), ref (region), (len + 1us), 1us);
        #{
            if (region.len <= 0us) panic;
            region [0us]
        #} = '-'c8;
        {
            let mut #_iter: usize = 0us;
            let #_nb_iters: usize = len;
            while (#_iter < #_nb_iters) {
                let mut i: usize = 0us;
                i = #_iter;
                {
                    #{
                        if (region.len <= ((region.len - i) - 1us)) panic;
                        region [((region.len - i) - 1us)]
                    #} = cast!{c8}((48u8 + cast!{u8}(-(m % b))));
                    m = (m / b);
                    <unit-value>
                }
            } step #_iter = (#_iter + 1us)
        }
    } else {
        std::conv::utils::Grow!{c8}::Grow::_yrt_grow_slice (ref (res), ref (region), len, 1us);
        {
            let mut #_iter: usize = 0us;
            let #_nb_iters: usize = len;
            while (#_iter < #_nb_iters) {
                let mut i: usize = 0us;
                i = #_iter;
                {
                    #{
                        if (region.len <= ((region.len - i) - 1us)) panic;
                        region [((region.len - i) - 1us)]
                    #} = cast!{c8}((48u8 + cast!{u8}(-(m % b))));
                    m = (m / b);
                    <unit-value>
                }
            } step #_iter = (#_iter + 1us)
        }
    }
}
frame: [weak] std::conv::to!{[c8], c8, 10, i32, i32}::to (a: i32, upper: bool = false)-> mut [mut c8] {
    if (a == 0) {
        return copy ("0"s8);
    };
    let sign: bool = (a < 0);
    let negVal: i32 = if sign a else -a;
    let b: i32 = 10;
    let mut n: mut i32 = negVal;
    let mut len: mut usize = 0us;
    while (n < 0) {
        len = (len + 1us);
        n = (n / b);
        <unit-value>
    };
    if !sign {
        let mut res: mut [mut c8] = core::types::array::allocArray!{c8}::allocArray (len);
        let mut m: mut i32 = negVal;
        {
            let mut #_iter: usize = 0us;
            let #_nb_iters: usize = len;
            while (#_iter < #_nb_iters) {
                let mut i: usize = 0us;
                i = #_iter;
                {
                    #{
                        if (res.len <= ((res.len - i) - 1us)) panic;
                        res [((res.len - i) - 1us)]
                    #} = cast!{c8}((48u8 + cast!{u8}(-(m % b))));
                    m = (m / b);
                    <unit-value>
                }
            } step #_iter = (#_iter + 1us)
        };
        return alias (res);
    } else {
        let mut res: mut [mut c8] = core::types::array::allocArray!{c8}::allocArray ((len + 1us));
        #{
            if (res.len <= 0us) panic;
            res [0us]
        #} = '-'c8;
        let mut m: mut i32 = negVal;
        {
            let mut #_iter: usize = 0us;
            let #_nb_iters: usize = len;
            while (#_iter < #_nb_iters) {
                let mut i: usize = 0us;
                i = #_iter;
                {
                    #{
                        if (res.len <= ((res.len - i) - 1us)) panic;
                        res [((res.len - i) - 1us)]
                    #} = cast!{c8}((48u8 + cast!{u8}(-(m % b))));
                    m = (m / b);
                    <unit-value>
                }
            } step #_iter = (#_iter + 1us)
        };
        return alias (res);
    };
}
frame: [weak] std::conv::to!{[c8], c8, i32}::to (a: i32, mut ref res: mut [mut c8])-> void {
    std::conv::to!{[c8], c8, 10, i32, i32}::to (a, ref (res), false);
    <unit-value>
}
frame: [weak] std::conv::to!{[c8], c8, i32}::to (a: i32)-> mut [mut c8] return {
    alias (std::conv::to!{[c8], c8, 10, i32, i32}::to (a, false))
}
frame: [weak] std::stream::StringStream::write!{test1::Foo}::write (mut self: mut &(mut std::stream::StringStream), ref c: test1::Foo)-> mut &(mut std::stream::StringStream) return {
    {
        std::stream::StringStream::write!{[c8], c8}::write(self:, "test1::Foo"s8, '('c8);
        cte for {
            {
                let i: usize = 0us;
                let j: i32 = c.x;
                {
                    std::stream::StringStream::write!{i32}::write(self:, j);
                    <unit-value>
                }
            }
        };
        std::stream::StringStream::write(self:, ')'c8);
        <unit-value>
    };
    alias (self)
}
frame: [weak] std::stream::StringStream::write!{[c8], c8}::write (mut self: mut &(mut std::stream::StringStream), c: [c8])-> mut &(mut std::stream::StringStream) return {
    std::stream::StringStream::push(self:, c);
    alias (self)
}
frame: [weak] std::stream::StringStream::write!{[c8], c8}::write (mut self: mut &(mut std::stream::StringStream), f: [c8], r: c8)-> mut &(mut std::stream::StringStream) return {
    std::stream::StringStream::write!{[c8], c8}::write(self:, f);
    {
        std::stream::StringStream::write(self:, r);
        <unit-value>
    };
    alias (self)
}
frame: [weak] std::stream::StringStream::write!{i32}::write (mut self: mut &(mut std::stream::StringStream), c: i32)-> mut &(mut std::stream::StringStream) return {
    {
        if (self._entabingLen != 0us) {
            return std::stream::StringStream::write!{[c8], c8}::write(self:, std::conv::to!{[c8], c8, i32}::to (c));
        };
        std::conv::to!{[c8], c8, i32}::to (c, ref (self._content));
        self._willEntab = ((self._content.len != 0us) && (#{
            if (self._content.len <= (self._content.len - 1us)) panic;
            self._content [(self._content.len - 1us)]
        #} == '\n'c8));
        alias (self)
    }
}
frame: [weak] core::types::array::allocArray!{c8}::allocArray (len: usize)-> mut [mut c8] return {
    let mut res: mut [mut c8] = {.len = 0us, .ptr = null};
    core::types::array::YRUNTIME!{c8}::YRUNTIME::_yrt_alloc_slice_no_set (ref (res), len, 1us);
    alias (res)
}
frame: test1::Foo::self (mut ref self: mut test1::Foo, x: i32)-> void {
    self.x = x;
    <unit-value>
}
frame: test1::main ()-> void {
    let x: i32 = 12;
    let y: test1::Foo = test1::Foo::self(new, 12);
    let str: [c8] = std::conv::to!{[c8], c8, i32}::to (x) ~ ", "s8 ~ std::conv::to!{[c8], c8, test1::Foo}::to (ref (y)) ~ ", "s8 ~ std::conv::to!{[c8], c8, i32}::to ((x + y.x));
    if !core::types::array::opEquals (str, "12, Foo(12), 24"s8) throw core::exception::assertion::AssertError::self(new, ""s8);
    <unit-value>
}
