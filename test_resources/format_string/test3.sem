frame: [weak] std::conv::to!{[c16], c16, 10, i32, i32}::to (a: i32, upper: bool = false)-> mut [mut c16] {
    if (a == 0) {
        return copy ("0"s16);
    };
    #{
        let mut n: mut i32 = if (a < 0) (-a, 0us, true) else (a, 0us, false).0;
        let mut len: mut usize = if (a < 0) (-a, 0us, true) else (a, 0us, false).1;
        let sign: bool = if (a < 0) (-a, 0us, true) else (a, 0us, false).2
    #};
    let absVal: i32 = n;
    let b: i32 = 10;
    while (n > 0) {
        len = (len + 1us);
        n = (n / b);
        <unit-value>
    };
    if !sign {
        let mut res: mut [mut c16] = core::types::array::allocArray!{c16}::allocArray (len);
        let mut m: mut i32 = absVal;
        {
            let mut #_iter: usize = 0us;
            let #_nb_iters: usize = {
                    let #_range: (mut usize, usize, isize) = (len, 0us, if (len <= 0us) 1is else -1is);
                    let #_val: (mut usize, mut usize, mut usize, mut usize) = if (#_range.2 < 0is) (-#_range.2, #_range.1, #_range.0, (#_range.0 - #_range.1)) else (#_range.2, #_range.0, #_range.1, (#_range.1 - #_range.0));
                    let #_nbAll: mut usize = (#_val.3 / #_val.0);
                    if (#_nbAll == 18446744073709551615us) #_nbAll else if (((#_nbAll * #_val.0) + #_val.1) != #_val.2) (#_nbAll + 1us) else #_nbAll
                };
            while (#_iter < #_nb_iters) {
                let mut i: usize = 0us;
                i = (len + (#_iter * cast!{usize}(if (len <= 0us) 1is else -1is)));
                {
                    #{
                        if (res.len <= (i - 1us)) panic;
                        res [(i - 1us)]
                    #} = cast!{c16}(cast!{c8}((48u8 + cast!{u8}((m % b)))));
                    m = (m / b);
                    <unit-value>
                }
            } step #_iter = (#_iter + 1us)
        };
        return alias (res);
    } else {
        let mut res: mut [mut c16] = core::types::array::allocArray!{c16}::allocArray ((len + 1us));
        #{
            if (res.len <= 0us) panic;
            res [0us]
        #} = '-'c16;
        let mut m: mut i32 = absVal;
        {
            let mut #_iter: usize = 0us;
            let #_nb_iters: usize = {
                    let #_range: (mut usize, usize, isize) = (len, 0us, if (len <= 0us) 1is else -1is);
                    let #_val: (mut usize, mut usize, mut usize, mut usize) = if (#_range.2 < 0is) (-#_range.2, #_range.1, #_range.0, (#_range.0 - #_range.1)) else (#_range.2, #_range.0, #_range.1, (#_range.1 - #_range.0));
                    let #_nbAll: mut usize = (#_val.3 / #_val.0);
                    if (#_nbAll == 18446744073709551615us) #_nbAll else if (((#_nbAll * #_val.0) + #_val.1) != #_val.2) (#_nbAll + 1us) else #_nbAll
                };
            while (#_iter < #_nb_iters) {
                let mut i: usize = 0us;
                i = (len + (#_iter * cast!{usize}(if (len <= 0us) 1is else -1is)));
                {
                    #{
                        if (res.len <= i) panic;
                        res [i]
                    #} = cast!{c16}(cast!{c8}((48u8 + cast!{u8}((m % b)))));
                    m = (m / b);
                    <unit-value>
                }
            } step #_iter = (#_iter + 1us)
        };
        return alias (res);
    };
}
frame: [weak] std::conv::to!{[c16], c16, i32}::to (a: i32)-> mut [mut c16] return {
    alias (std::conv::to!{[c16], c16, 10, i32, i32}::to (a, false))
}
frame: [weak] std::conv::to!{[c32], c32, 10, i32, i32}::to (a: i32, upper: bool = false)-> mut [mut c32] {
    if (a == 0) {
        return copy ("0"s32);
    };
    #{
        let mut n: mut i32 = if (a < 0) (-a, 0us, true) else (a, 0us, false).0;
        let mut len: mut usize = if (a < 0) (-a, 0us, true) else (a, 0us, false).1;
        let sign: bool = if (a < 0) (-a, 0us, true) else (a, 0us, false).2
    #};
    let absVal: i32 = n;
    let b: i32 = 10;
    while (n > 0) {
        len = (len + 1us);
        n = (n / b);
        <unit-value>
    };
    if !sign {
        let mut res: mut [mut c32] = core::types::array::allocArray!{c32}::allocArray (len);
        let mut m: mut i32 = absVal;
        {
            let mut #_iter: usize = 0us;
            let #_nb_iters: usize = {
                    let #_range: (mut usize, usize, isize) = (len, 0us, if (len <= 0us) 1is else -1is);
                    let #_val: (mut usize, mut usize, mut usize, mut usize) = if (#_range.2 < 0is) (-#_range.2, #_range.1, #_range.0, (#_range.0 - #_range.1)) else (#_range.2, #_range.0, #_range.1, (#_range.1 - #_range.0));
                    let #_nbAll: mut usize = (#_val.3 / #_val.0);
                    if (#_nbAll == 18446744073709551615us) #_nbAll else if (((#_nbAll * #_val.0) + #_val.1) != #_val.2) (#_nbAll + 1us) else #_nbAll
                };
            while (#_iter < #_nb_iters) {
                let mut i: usize = 0us;
                i = (len + (#_iter * cast!{usize}(if (len <= 0us) 1is else -1is)));
                {
                    #{
                        if (res.len <= (i - 1us)) panic;
                        res [(i - 1us)]
                    #} = cast!{c32}(cast!{c8}((48u8 + cast!{u8}((m % b)))));
                    m = (m / b);
                    <unit-value>
                }
            } step #_iter = (#_iter + 1us)
        };
        return alias (res);
    } else {
        let mut res: mut [mut c32] = core::types::array::allocArray!{c32}::allocArray ((len + 1us));
        #{
            if (res.len <= 0us) panic;
            res [0us]
        #} = '-'c32;
        let mut m: mut i32 = absVal;
        {
            let mut #_iter: usize = 0us;
            let #_nb_iters: usize = {
                    let #_range: (mut usize, usize, isize) = (len, 0us, if (len <= 0us) 1is else -1is);
                    let #_val: (mut usize, mut usize, mut usize, mut usize) = if (#_range.2 < 0is) (-#_range.2, #_range.1, #_range.0, (#_range.0 - #_range.1)) else (#_range.2, #_range.0, #_range.1, (#_range.1 - #_range.0));
                    let #_nbAll: mut usize = (#_val.3 / #_val.0);
                    if (#_nbAll == 18446744073709551615us) #_nbAll else if (((#_nbAll * #_val.0) + #_val.1) != #_val.2) (#_nbAll + 1us) else #_nbAll
                };
            while (#_iter < #_nb_iters) {
                let mut i: usize = 0us;
                i = (len + (#_iter * cast!{usize}(if (len <= 0us) 1is else -1is)));
                {
                    #{
                        if (res.len <= i) panic;
                        res [i]
                    #} = cast!{c32}(cast!{c8}((48u8 + cast!{u8}((m % b)))));
                    m = (m / b);
                    <unit-value>
                }
            } step #_iter = (#_iter + 1us)
        };
        return alias (res);
    };
}
frame: [weak] std::conv::to!{[c32], c32, i32}::to (a: i32)-> mut [mut c32] return {
    alias (std::conv::to!{[c32], c32, 10, i32, i32}::to (a, false))
}
frame: [weak] core::types::array::allocArray!{c16}::allocArray (len: usize)-> mut [mut c16] return {
    let mut res: mut [mut c16] = {.len = 0us, .ptr = null};
    core::types::array::YRUNTIME!{c16}::YRUNTIME::_yrt_alloc_slice_no_set (ref (res), len, 2us);
    alias (res)
}
frame: [weak] core::types::array::allocArray!{c32}::allocArray (len: usize)-> mut [mut c32] return {
    let mut res: mut [mut c32] = {.len = 0us, .ptr = null};
    core::types::array::YRUNTIME!{c32}::YRUNTIME::_yrt_alloc_slice_no_set (ref (res), len, 4us);
    alias (res)
}
frame: test3::main ()-> void {
    let x: i32 = 12;
    let str16: [c16] = std::conv::to!{[c16], c16, i32}::to (x);
    let str32: [c32] = std::conv::to!{[c32], c32, i32}::to (x);
    <unit-value>
}
