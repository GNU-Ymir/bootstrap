frame : test15::main ()-> void {
    let mut #_value : (i32)? = test15::foo ();
    if (#_value.hasValue && #{
        let x : i32 = #_value.value;
        (x == 12)
    #}) {
        <unit-value>
    } else if (#_value.hasValue && #{
        let x : i32 = #_value.value;
        ((x != 3) && (x != 4))
    #}) {
        <unit-value>
    } else {
        <unit-value>
    }
}
