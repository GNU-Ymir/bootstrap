class A {
    pub let x : i32;

    pub self (x : i32) with x = x {}

    @field
    pub fn foo (self)-> i32 {
        1
    }
}

fn main () {
    let a = A::new (2);
    let A (foo-> x, x-> x) = a;
}
