frame : test25::main ()-> void {
    let mut a : mut [mut i32] = copy ({.len = 1us, .ptr = &([12])});
    {
        let ref mut #_value : mut i32 = ref (#{
                    if (a.len <= 0us) panic;
                    a [0us]
                #});
        if #{
            let ref mut x : mut i32 = ref (#_value);
            (x == 2)
        #} {
            x = 98;
            <unit-value>
        } else if ((#_value == 12) && #{
            let ref mut z : mut i32 = ref (#_value);
            true
        #}) {
            z = 87;
            <unit-value>
        }
    }
}
