frame : test1::main ()-> void {
    let a : [i32] = copy (([1, 2, 3])[]);
    let b : i32 = #{
            if (a.len <= 0us) core::array::outOfArray ();
            a [0us]
        #};
    let c : [i32] = #{
            if (a.len < 2) core::array::outOfArray ();
            ({.len = 2us, .ptr = (a.ptr + 0us)})[]
        #};
    #{
        if (c.len <= 3us) core::array::outOfArray ();
        c [3us]
    #};
    b;
    <unit-value>
}
