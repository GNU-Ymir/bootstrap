// The same parameters written by position: the call is short of the arity it
// needs, and each marked parameter says why.

fn foo(@a: i32, @b: i32, c: i32) {
    a;
    b;
    c;
}

fn main() {
    foo(98, 12, 23);
}
