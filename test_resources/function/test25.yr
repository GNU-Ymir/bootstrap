// A call whose arity is right, the marked parameters simply left unnamed: only
// the naming errors are reported, no count is compared.

fn foo(@a: i32, @b: i32, c: i32) {
    a;
    b;
    c;
}

fn main() {
    foo(98);
}
