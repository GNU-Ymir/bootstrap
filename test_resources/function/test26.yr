// The marker survives template instantiation: the parameter of the instantiated
// function can still only be passed by name.

fn foo {T} (@a: T, b: T)-> T {
    a + b
}

fn main () {
    let _ok_ = foo (2, a-> 1);
    let _ko_ = foo (1, 2);
}
