frame : test4::main ()-> void {
    let mut a : mut i32 = 1;
    a = (a + 2);
    a = 90;
    let mut b : mut usize = 10us;
    b = (b / 2us);
    let mut c : mut isize = 10is;
    c = (c * 9is);
    c = (c * 6is);
    c = (c % 3is);
    <unit-value>
}
