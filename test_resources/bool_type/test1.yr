mod test1;

fn main () {
    bool::init;
    bool::typeid;

    let a = true;

    let mut b = a;
    b = true;

    a || true;
    a && false;
    !a;

    a == true;
    a != false;

    cte {a || false};
    cte {a || true};
    cte {a && false};
    cte {a && true};
    cte {!a};

    cte{a == true};
    cte{a != false};

}
