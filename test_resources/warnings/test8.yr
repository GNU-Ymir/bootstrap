// naming a symbol by its full path resolves nothing through the use, the module
// is a submodule of the package and is named without it

in test8;

mod ::lib;

use test8::lib;

fn main () {
    lib::value ();
}
