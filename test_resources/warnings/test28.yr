// a module name several uses bring credits the one the field accessed with `::` was
// found in

in test28;

mod ::a;
mod ::b;

use test28::a::_;
use test28::b::sub;

fn main () {
    sub::foo ();
    sub::bar ();
}
