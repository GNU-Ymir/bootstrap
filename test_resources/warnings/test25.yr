// a lookup through an imported module searches the modules enclosing it, so a
// symbol found there credits the use

in test25;

mod ::gen;

use test25::gen::ty::native;

fn foo (_a_ : &Ty) {}

fn main () {}
