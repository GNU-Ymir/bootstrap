in alpha;

pub fn conv {T, U of i32} (x : U)-> T { cast!T (x) }

pub record Point {
    pub let x : i32;
    pub let y : i32;

    pub self () with x = 0, y = 0 {}
    pub self (x : i32) with x = x, y = 0 {}
    pub self (x : i32, y : i32) with x = x, y = y {}
}
