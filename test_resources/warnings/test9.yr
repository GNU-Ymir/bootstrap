// overload resolution picks one candidate, the use that offered the other one
// resolved nothing

in test9;

mod ::alpha;
mod ::beta;

use test9::alpha;
use test9::beta;

fn main () {
    pick (1);
}
