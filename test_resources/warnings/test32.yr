// an index the optimizer propagates into the check of a static array is still
// reported, before the comparison is folded away
fn foo ()-> i32 {
    let a = [1, 2, 3];
    let mut i = 10us;
    a [i]
}
