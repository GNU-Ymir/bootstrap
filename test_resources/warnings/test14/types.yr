in types;

pub record Multi {
    pub let a : u64;
    pub let b : u64;

    pub self () with a = 0u64, b = 0u64 {}
    pub self (a : u64) with a = a, b = 0u64 {}
    pub self (a : u64, b : u64) with a = a, b = b {}
}
