// an overloaded construction leaves the resolution to the call, and still credits
// the name every candidate came from

in test27;

mod ::syn;

use test27::syn::vis;

fn main () {
    let _a_ = copy Visitor (12);
}
