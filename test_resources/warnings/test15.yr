// several templates answer a call written with explicit template arguments, the
// one the overload resolution keeps is resolved through the use

in test15;

mod ::lib;

use test15::lib;

fn main () {
    let _a_ : i64 = conv!{i64} (12);
}
