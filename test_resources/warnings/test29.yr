// a module name several uses bring credits the one the type accessed with `::` was
// found in

in test29;

mod ::a;
mod ::b;

use test29::a::_;
use test29::b::sub;

fn foo (_x_ : &sub::Foo, _y_ : &sub::Bar) {}

fn main () {}
