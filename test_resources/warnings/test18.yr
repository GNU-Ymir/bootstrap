// the template of the submodule is instantiated, so the module is fully
// validated and the use its body never resolves through is reported

in test18;

mod ::traits;
mod ::guarded;

use test18::guarded;

fn main () {
    let _a_ = bar (12);
}
