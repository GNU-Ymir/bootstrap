// the macro of the submodule is expanded, so the module is fully validated and
// the use its body never resolves through is reported

in test20;

mod ::helpers;
mod ::mac;

use test20::mac;

fn main () {
    let _a_ = twice#{ 1 };
}
