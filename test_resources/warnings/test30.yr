// an overload found through several modules of the same name is left to the call,
// which can select any of them

in test30;

mod ::a;
mod ::b;

use test30::a::_;
use test30::b::sub;

fn main () {
    sub::baz (1);
}
