// a name in type position credits the module the `_` use globbed, the only route
// to a symbol that module declares itself

in test23;

mod ::gen;

use test23::gen::_;

fn foo (_a_ : &Gener, _b_ : &Sub) {}

fn main () {}
