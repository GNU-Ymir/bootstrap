// a record is constructed without copy, so the construction is the only thing
// that can resolve it through the use, and it does

in test14;

mod ::types;

use test14::types;

fn main () {
    let _a_ = Multi (1u64, 2u64);
}
