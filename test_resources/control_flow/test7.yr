class A {
    pub let i : i32;
    pub self (i : i32) with i = i {}

    @field
    pub fn len (self)-> i32 {
        2
    }
}

fn main () {
    let a = copy A (1);
    let A (i-> x, len-> y) = a;
    x; y;
}
