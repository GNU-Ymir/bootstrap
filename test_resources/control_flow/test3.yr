fn main () {
    let a = [1, 2, 3];
    if let [x, y, z] = a { // error, len of /a/ is /cte/
        x;
        y;
        z;
    }

    if (a.len <= 3) {
    }

    if (a.len >= 4) {
    }

    do {
    } while a.len > 2;

    do {
    } while a.len > 4;

    while a.len <= 3 {
    }

    while a.len >= 4 {
    }

}
