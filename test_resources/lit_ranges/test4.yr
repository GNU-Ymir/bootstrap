fn main () {
    let a = ..i32::init;
    let _b_ = ..i32::typeid;

    cte assert (a.fst == 0);
    cte assert (a.scd == 0);
    cte assert (a.step == 1); // (0 <= 0) => step = 1
    cte assert (a.contains == false);
}
