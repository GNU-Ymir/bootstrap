// the reverse of an empty range is empty, even on an unsigned type
fn main () {
    cte assert ((0 .. 0).reverse ().len == 0us);
    cte assert ((0us .. 0us).reverse ().len == 0us);
    cte assert ((10us .. 0us).reverse ().len == 0us);
    cte assert ((0us ... 0us).reverse ().len == 1us);
}
