frame: [weak] core::types::range::stepBy!{i32, i32}::stepBy (x: (..i32), s: i32)-> (..i32) return {
    let mut z: mut (..mut i32) = x;
    z.step = s;
    z
}
frame: test15::main ()-> void {
    let n: i32 = 3;
    let r: (..i32) = {
            let #_range: (..i32) = core::types::range::stepBy!{i32, i32}::stepBy ((1 ..[false] 10).step_by(1), n);
            let #_len: usize = cast!{usize}({
                    let #_range: (i32, i32, i32) = (#_range.fst, #_range.scd, #_range.step);
                    let #_val: (i32, i32, i32, i32) = if (#_range.2 < 0) (-#_range.2, #_range.1, #_range.0, (#_range.0 - #_range.1)) else (#_range.2, #_range.0, #_range.1, (#_range.1 - #_range.0));
                    let #_nbAll: i32 = (#_val.3 / #_val.0);
                    if (#_val.1 > #_val.2) 0 else if (#_nbAll == 18446744073709551615) #_nbAll else if ((((#_nbAll * #_val.0) + #_val.1) != #_val.2) || #_range.contains) (#_nbAll + 1) else #_nbAll
                });
            (if (#_len == 0us) #_range.fst else (#_range.fst + cast!{i32}(((#_len - 1us) * cast!{usize}(#_range.step)))) ..[(#_len != 0us)] #_range.fst).step_by(-#_range.step)
        };
    {
        let mut #_iter: usize = 0us;
        let #_nb_iters: usize = {
                let #_range: (i32, i32, i32) = (r.fst, r.scd, r.step);
                let #_val: (i32, i32, i32, i32) = if (#_range.2 < 0) (-#_range.2, #_range.1, #_range.0, (#_range.0 - #_range.1)) else (#_range.2, #_range.0, #_range.1, (#_range.1 - #_range.0));
                let #_nbAll: i32 = (#_val.3 / #_val.0);
                if (#_val.1 > #_val.2) 0 else if (#_nbAll == 18446744073709551615) #_nbAll else if ((((#_nbAll * #_val.0) + #_val.1) != #_val.2) || r.contains) (#_nbAll + 1) else #_nbAll
            };
        let #_fst: i32 = r.fst;
        let #_step: i32 = r.step;
        while (#_iter < #_nb_iters) {
            let mut i: i32 = 0;
            i = (#_fst + cast!{i32}((#_iter * cast!{usize}(#_step))));
            {
                i;
                <unit-value>
            }
        } step #_iter = (#_iter + 1us)
    }
}
