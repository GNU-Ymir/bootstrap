frame: test9::main ()-> void {
    let mut a: mut usize = 10us;
    {
        let mut #_iter: usize = 0us;
        let #_nb_iters: usize = a;
        while (#_iter < #_nb_iters) {
            {
                <unit-value>
            }
        } step #_iter = (#_iter + 1us)
    };
    {
        let mut #_iter: usize = 0us;
        let #_nb_iters: usize = if (a > 0us) 0us else if ((-a + a) != 0us) (-a + 1us) else -a;
        let #_fst: usize = a;
        while (#_iter < #_nb_iters) {
            {
                <unit-value>
            }
        } step #_iter = (#_iter + 1us)
    };
    {
        let mut #_iter: usize = 0us;
        let #_nb_iters: usize = a;
        while (#_iter < #_nb_iters) {
            {
                <unit-value>
            }
        } step #_iter = (#_iter + 1us)
    };
    {
        let mut #_iter: usize = 0us;
        let #_nb_iters: usize = if (a > 0us) 0us else if ((-a + a) != 0us) (-a + 1us) else -a;
        let #_fst: usize = a;
        while (#_iter < #_nb_iters) {
            {
                <unit-value>
            }
        } step #_iter = (#_iter + 1us)
    }
}
