frame: test16::main ()-> void {
    let mut n: mut usize = 4us;
    n = (n + 1us);
    {
        let mut #_iter: usize = 0us;
        let #_nb_iters: usize = {
                let #_range: (usize, usize, isize) = (if (n == 0us) 0us else (n - 1us), 0us, -1is);
                let #_val: (usize, usize, usize, usize) = if (#_range.2 < 0is) (-#_range.2, #_range.1, #_range.0, (#_range.0 - #_range.1)) else (#_range.2, #_range.0, #_range.1, (#_range.1 - #_range.0));
                let #_nbAll: usize = (#_val.3 / #_val.0);
                if (#_val.1 > #_val.2) 0us else if (#_nbAll == 18446744073709551615us) #_nbAll else if ((((#_nbAll * #_val.0) + #_val.1) != #_val.2) || (n != 0us)) (#_nbAll + 1us) else #_nbAll
            };
        let #_fst: usize = if (n == 0us) 0us else (n - 1us);
        while (#_iter < #_nb_iters) {
            let mut i: usize = 0us;
            i = (#_fst - #_iter);
            {
                i;
                <unit-value>
            }
        } step #_iter = (#_iter + 1us)
    }
}
