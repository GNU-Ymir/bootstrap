mod test1;

fn main () {
    let a : ..i32 = 0 .. 2;
    let b : ..i32 = 0 ... 2;

    cte assert (a.fst == b.fst);
    cte assert (a.scd == b.scd);
    cte assert (a.step == b.step);
    cte assert (a.contain != b.contain);
}
