frame: test8::main ()-> void {
    let mut a: mut [mut i32] = {
            let mut #_iter: usize = 0us;
            let mut i: i32 = 0;
            let mut #_value: mut [mut i32] = copy [<unit-value> ; 10us];
            while (#_iter < 10us) {
                i = cast!{i32}(#_iter);
                #_value [#_iter] = i;
                #_iter = (#_iter + 1us)
            };
            #_value
        };
    let f: i32 = #{
            if (a.len <= 0us) panic;
            a [0us]
        #};
    let j: i32 = #{
            if (a.len <= 1us) panic;
            a [1us]
        #};
    a = alias (#{
            if (a.len < 2us) panic;
            {.len = (a.len - 2us), .ptr = (a.ptr + (2us * 4us)), .blk = a.blk_info}
        #});
    {
        let mut #_iter: usize = 0us;
        let #_nb_iters: usize = if (f > j) 0 else if (((j - f) + f) != j) ((j - f) + 1) else (j - f);
        let #_fst: i32 = f;
        while (#_iter < #_nb_iters) {
            let mut z: i32 = 0;
            z = (#_fst + cast!{i32}(#_iter));
            {
                z;
                <unit-value>
            }
        } step #_iter = (#_iter + 1us)
    }
}
