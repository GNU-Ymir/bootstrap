frame: [weak] core::types::range::opContains!{i32}::opContains (range: (..i32), x: i32)-> bool return {
    let zero: i32 = 0;
    if (range.step < zero) ((x <= range.fst) && ((x > range.scd) || (range.contains && (x == range.scd)))) else ((x >= range.fst) && ((x < range.scd) || (range.contains && (x == range.scd))))
}
frame: test5::main ()-> void {
    <unit-value>
}
