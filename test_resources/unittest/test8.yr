// the parameter sets do not fill every parameter of the test
fn params ()-> [(i32, [c8])] {
    copy [(3, "foo")]
}

@parameters (params ())
__test (len: i32, text: [c8], ratio: f64) {
    assert (len > 0 && text.len > 0us && ratio > 0.0);
}
