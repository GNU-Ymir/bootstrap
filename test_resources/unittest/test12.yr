// the parameters are the values a generator yields, not the generator itself
yield params ()-> i32 {
    yield 1;
    yield 2;
}

@parameters (params)
__test (value: i32) {
    assert (value > 0);
}
