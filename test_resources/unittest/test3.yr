// a test run once per parameter set, produced by a function
fn params ()-> [(i32, [c8])] {
    copy [(3, "foo"), (2, "bar")]
}

@parameters (params ())
__test named (len: i32, text: [c8]) {
    assert (text.len == cast!usize (len));
}
