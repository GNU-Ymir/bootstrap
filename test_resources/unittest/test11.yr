// a parameter is unwrapped from a parameter set, so it can carry no modifier
yield params ()-> [i32] {
    yield copy [1, 2];
    yield copy [3];
}

@parameters (params ())
__test (dmut a: [i32]) {
    a [0] = 42;
}
