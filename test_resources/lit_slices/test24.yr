// The size of a static allocation must be known at compile time; a dynamic
// slice is what a runtime size asks for, and that is what copy gives.
fn main () {
    let mut n = 3;
    n += 1;

    let _ = [0 ; n];
}
