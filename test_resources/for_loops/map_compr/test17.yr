// a tuple is iterated at compile time, but its pairs are inserted at runtime,
// so the filter guarding them does not have to be known at compile time
fn main (args: [[c8]]) {
    let n = args.len;
    let dmut a = copy [i => x for i, x in (1, 2, 3) if i != n];
    a [0us];
}
