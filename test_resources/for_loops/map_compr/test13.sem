frame: [weak] core::types::map::compareKeys!{i32}::compareKeys (a: *i32, b: *i32)-> bool {
    return (*(a) == *(b));
}
frame: [weak] core::types::map::hashKey!{i32}::hashKey (a: *i32)-> u64 return {
    core::types::hash::__hash (*(a))
}
frame: test13::main ()-> void {
    let mut a: mut [i32 => mut [mut i32]] = {
            let mut #_iter: usize = 0us;
            let mut x: i32 = 0;
            let mut #_value: mut [i32 => mut [mut i32]] = [];
            while (#_iter < 3us) {
                x = cast!{i32}(#_iter);
                #_value [x] = {
                    let mut #_iter: usize = 0us;
                    let #_nb_iters: usize = if (0 > x) 0 else x;
                    let mut y: i32 = 0;
                    let mut #_value: mut [mut i32] = copy [<unit-value> ; if (0 > x) 0 else x];
                    while (#_iter < #_nb_iters) {
                        y = cast!{i32}(#_iter);
                        #_value [#_iter] = y;
                        #_iter = (#_iter + 1us)
                    };
                    #_value
                };
                #_iter = (#_iter + 1us)
            };
            #_value
        };
    a [1];
    <unit-value>
}
