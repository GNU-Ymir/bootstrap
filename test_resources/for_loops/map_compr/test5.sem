frame: [weak] core::types::map::compareKeys!{i32}::compareKeys (a: *i32, b: *i32)-> bool {
    return (*(a) == *(b));
}
frame: [weak] core::types::map::hashKey!{i32}::hashKey (a: *i32)-> u64 return {
    core::types::hash::__hash (*(a))
}
frame: test5::main ()-> void {
    let mut src: mut [i32 => mut i32] = [1 => 2, 2 => 3];
    let mut a: mut [i32 => mut i32] = {
            let mut #_value: mut [i32 => mut i32] = [];
            {
                let mut #_iter: #_iterator!{mut [i32 => mut i32]} = #_begin(src);
                try {
                    while #_not_end(#_iter) {
                        let ref k: i32 = ref (#_key(#_iter));
                        let v: i32 = #_value(#_iter);
                        #_value [v] = k;
                        #_next(#_iter)
                    }
                } finally {
                    {
                        #_del(#_iter)
                    }
                }
            };
            #_value
        };
    a [2];
    <unit-value>
}
