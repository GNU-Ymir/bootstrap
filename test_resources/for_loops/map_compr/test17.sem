frame: [weak] core::types::map::compareKeys!{usize}::compareKeys (a: *usize, b: *usize)-> bool {
    return (*(a) == *(b));
}
frame: [weak] core::types::map::hashKey!{usize}::hashKey (a: *usize)-> u64 return {
    core::types::hash::__hash (*(a))
}
frame: test17::main (args: [[c8]])-> void {
    let n: usize = args.len;
    let mut a: mut [usize => mut i32] = {
            let mut #_value: mut [usize => mut i32] = [];
            {
                let i: usize = 0us;
                let x: i32 = 1;
                if (i != n) #_value [i] = x
            };
            {
                let i: usize = 1us;
                let x: i32 = 2;
                if (i != n) #_value [i] = x
            };
            {
                let i: usize = 2us;
                let x: i32 = 3;
                if (i != n) #_value [i] = x
            };
            #_value
        };
    a [0us];
    <unit-value>
}
