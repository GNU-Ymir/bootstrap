// the filtered map comprehension of the roadmap
fn foo (x: i32)-> i32 { x * x }
fn keep (x: i32)-> bool { (x % 2) == 0 }

fn main () {
    let dmut a = copy [x => foo (x) for x in 0 .. 10 if keep (x)];
    a [4];
}
