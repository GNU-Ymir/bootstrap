frame: test25::main ()-> void {
    let src: [i32 ; 4us] = [1, 2, 3, 4];
    let _: [i32] = {
            let #_value_init: [i32] = {.len = 4us, .ptr = &(src)};
            let mut #_value: mut [mut i32] = copy [<unit-value> ; 0us];
            {
                let mut #_index: usize = 0us;
                while (#_index < 4us) {
                    let x: i32 = #_value_init [#_index];
                    if (x != 2) #_value ~= {.len = 1us, .ptr = &([x])};
                    #_index = (#_index + 1us)
                }
            };
            #_value
        };
    let _: [i32] = {
            let #_value_init: [i32] = {.len = 3us, .ptr = &([2, 3, 4])};
            let mut #_value: mut [mut i32] = copy [<unit-value> ; 0us];
            {
                let mut #_index: usize = 0us;
                while (#_index < 3us) {
                    let x: i32 = #_value_init [#_index];
                    if (x != 3) #_value ~= {.len = 1us, .ptr = &([(x * 2)])};
                    #_index = (#_index + 1us)
                }
            };
            #_value
        };
    <unit-value>
}
