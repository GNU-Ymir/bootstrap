frame: test21::A::self (mut self: mut &(mut test21::A), next: (dg ()-> void)[nullable])-> void {
    if (next.func != null) (next.func)(next.closure);
    <unit-value>
}
frame: test21::main ()-> void {
    let mut a: mut [mut &(mut test21::A)] = copy ({.len = 2us, .ptr = &([test21::A::self(new), test21::A::self(new)])});
    let mut _b_: mut [mut &(mut test21::A)] = {
            let #_value_init: mut [mut &(mut test21::A)] = a;
            let mut #_value: mut [mut &(mut test21::A)] = copy [<unit-value> ; #_value_init.len];
            {
                let mut #_index: usize = 0us;
                while (#_index < #_value_init.len) {
                    let mut i: mut &(mut test21::A) = alias (#_value_init [#_index]);
                    #_value [#_index] = alias (i);
                    #_index = (#_index + 1us)
                }
            };
            #_value
        };
    <unit-value>
}
