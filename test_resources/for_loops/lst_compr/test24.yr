// the filtered list comprehension of the roadmap
fn foo (x: i32)-> i32 { x * x }
fn keep (x: i32)-> bool { (x % 2) == 0 }

fn main () {
    let _ = copy [foo (x) for x in 0 .. 10 if keep (x)];
    let _ = copy [i for i in 0 .. 10 if i != 6];
}
