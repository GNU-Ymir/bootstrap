// the filter of an array comprehension over a tuple is computed at compile time
// too, so it cannot depend on a runtime value either
fn main (args: [[c8]]) {
    let n = args.len;
    let _ = [x for i, x in (1, 2, 3) if i != n];
}
