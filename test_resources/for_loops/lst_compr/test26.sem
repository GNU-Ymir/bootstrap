frame: test26::main ()-> void {
    let src: [i32 ; 3us] = [10, 20, 30];
    let _: [i32] = {
            let #_value_init: [i32] = {.len = 3us, .ptr = &(src)};
            let mut #_value: mut [mut i32] = copy [<unit-value> ; 0us];
            {
                let mut i: usize = 0us;
                while (i < 3us) {
                    let x: i32 = #_value_init [i];
                    if (i != 1us) #_value ~= {.len = 1us, .ptr = &([x])};
                    i = (i + 1us)
                }
            };
            #_value
        };
    <unit-value>
}
