// a filter over a slice, and over the array it is aliased from
fn main () {
    let src = [1, 2, 3, 4];
    let _ = copy [x for x in src if x != 2];
    let _ = copy [x * 2 for x in src [1 .. 4] if x != 3];
}
