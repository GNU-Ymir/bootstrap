frame: test22::A::self (mut self: mut &(mut test22::A), next: (dg ()-> void)[nullable])-> void {
    if (next.func != null) (next.func)(next.closure);
    <unit-value>
}
frame: test22::main ()-> void {
    let mut a: mut [mut &(mut test22::A)] = copy ({.len = 2us, .ptr = &([test22::A::self(new), test22::A::self(new)])});
    let mut _b_: (mut &(mut test22::A), mut &(mut test22::A)) = {
            let #_value_init: mut [mut &(mut test22::A)] = #{
                    if (a.len < 2us) panic;
                    {.len = 2us, .ptr = (a.ptr + (0us * 8us)), .blk = a.blk_info}
                #};
            let mut #_index: usize = 0us;
            ({
                    #_index = 0us;
                    let mut i: mut &(mut test22::A) = alias (#_value_init [#_index]);
                    alias (i)
                }, {
                    #_index = 1us;
                    let mut i: mut &(mut test22::A) = alias (#_value_init [#_index]);
                    alias (i)
                })
        };
    <unit-value>
}
