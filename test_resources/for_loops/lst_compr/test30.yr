// a tuple comprehension over a tuple, filtered on the index and on the element
fn main () {
    let src = (1, "a", 2);
    let _ = (x for i, x in src if i != 1us,);
    let _ = (i for i, _ in src if i != 0us,);
}
