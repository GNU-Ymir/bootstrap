// a tuple has a compile time arity, so the filter choosing its elements cannot
// depend on a runtime value
fn main (args: [[c8]]) {
    let n = args.len;
    let _ = (i for i in 0 .. 5 if i != n,);
}
