frame: test24::foo (x: i32)-> i32 return (x * x)
frame: test24::keep (x: i32)-> bool return ((x % 2) == 0)
frame: test24::main ()-> void {
    let _: [i32] = {
            let mut #_iter: usize = 0us;
            let mut x: i32 = 0;
            let mut #_value: mut [mut i32] = copy [<unit-value> ; 0us];
            while (#_iter < 10us) {
                x = cast!{i32}(#_iter);
                if test24::keep (x) #_value ~= {.len = 1us, .ptr = &([test24::foo (x)])};
                #_iter = (#_iter + 1us)
            };
            #_value
        };
    let _: [i32] = {
            let mut #_iter: usize = 0us;
            let mut i: i32 = 0;
            let mut #_value: mut [mut i32] = copy [<unit-value> ; 0us];
            while (#_iter < 10us) {
                i = cast!{i32}(#_iter);
                if (i != 6) #_value ~= {.len = 1us, .ptr = &([i])};
                #_iter = (#_iter + 1us)
            };
            #_value
        };
    <unit-value>
}
