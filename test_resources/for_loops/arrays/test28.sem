frame: test28::main ()-> void {
    let mut a: mut [mut i32 ; 4us] = [1, 2, 3, 4];
    cte for {
        {
            let i: usize = 0us;
            let elem: i32 = a [0us];
            {
                if (elem == 2) {
                    continue;
                };
                if (elem == 3) {
                    break ;
                };
                a [0us] = elem;
                <unit-value>
            }
        };
        {
            let i: usize = 1us;
            let elem: i32 = a [1us];
            {
                if (elem == 2) {
                    continue;
                };
                if (elem == 3) {
                    break ;
                };
                a [0us] = (elem + 1);
                <unit-value>
            }
        };
        {
            let i: usize = 2us;
            let elem: i32 = a [2us];
            {
                if (elem == 2) {
                    continue;
                };
                if (elem == 3) {
                    break ;
                };
                a [0us] = (elem + 2);
                <unit-value>
            }
        };
        {
            let i: usize = 3us;
            let elem: i32 = a [3us];
            {
                if (elem == 2) {
                    continue;
                };
                if (elem == 3) {
                    break ;
                };
                a [0us] = (elem + 3);
                <unit-value>
            }
        }
    }
}
