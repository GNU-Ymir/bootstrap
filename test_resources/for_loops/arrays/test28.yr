// break and continue jump from one unrolled iteration to the next
fn main () {
    let dmut a = [1, 2, 3, 4];
    cte for i, elem in a {
        if elem == 2 { continue; }
        if elem == 3 { break; }
        a [0] = elem + cast!i32 (i);
    }
}
