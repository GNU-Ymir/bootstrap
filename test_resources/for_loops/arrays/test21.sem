frame: test21::main ()-> void {
    let a: [i32 ; 3us] = [1, 2, 3];
    cte for {
        {
            let i: usize = 0us;
            let elem: i32 = 1;
            {
                let _: (usize, i32) = (i, elem);
                <unit-value>
            }
        };
        {
            let i: usize = 1us;
            let elem: i32 = 2;
            {
                let _: (usize, i32) = (i, elem);
                <unit-value>
            }
        };
        {
            let i: usize = 2us;
            let elem: i32 = 3;
            {
                let _: (usize, i32) = (i, elem);
                <unit-value>
            }
        }
    }
}
