// the index is a compile time value, usable where one is required
fn foo {T} (x : T)-> T {
    x
}

fn main () {
    let t = (1, 'r', "salut");
    cte for i, _ in [0, 1, 2] {
        let _ = foo (t.i);
    }
}
