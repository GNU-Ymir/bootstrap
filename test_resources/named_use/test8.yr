// Types are reached through the name as values are, root paths included.

pub mod A {
    pub class X {
        pub self () {}
    }

    pub fn foo ()-> i32 { 12 }
}

use IO = ::test8::A;

fn main () {
    let a : &IO::X = copy IO::X ();
    let b = IO::foo ();
    a; b;
}
