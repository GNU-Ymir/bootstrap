// The name binds the whole brace group, not a single module.

mod A {
    pub mod B {
        pub fn f ()-> i32 { 1 }
    }

    pub mod C {
        pub fn g ()-> f64 { 2.0 }
    }
}

use X = A::{B, C};

fn main () {
    let a = X::f ();
    let b = X::g ();
    a; b;
}
