// Two uses cannot write the same name, only the paths of a single use share it.

mod A {
    pub mod B {
        pub fn f ()-> i32 { 1 }
    }

    pub mod C {
        pub fn g ()-> f64 { 2.0 }
    }
}

use X = A::B;
use X = A::C;

fn main () {}
