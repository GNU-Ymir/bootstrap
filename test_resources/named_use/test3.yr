// The name binds the wildcard expansion, giving one prefix for the whole subtree.

mod A {
    pub mod B {
        pub fn f ()-> i32 { 1 }
    }

    pub mod C {
        pub fn g ()-> f64 { 2.0 }
    }
}

use X = A::_;

fn main () {
    let a = X::f ();
    let b = X::g ();
    a; b;
}
