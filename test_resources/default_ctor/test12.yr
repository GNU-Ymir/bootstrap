// A ctor written by hand with a different signature is no conflict, both stand.
class Foo {
   pub let x: i32;

   pub self dup (o: &Foo) with x = o.x {}
   pub self;
}

fn main () {
    let a = copy Foo (1);
    let b = copy Foo::dup (a);
    a.x; b.x;
}
