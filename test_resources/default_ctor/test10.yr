// A `@data` record asks for the ctor by its attribute, the request is implicit
// and is not redeclarable.
@data
record Foo {
    let x: i32;

    pub self;
}

fn main () {
    let a = Foo (1);
    a.x;
}
