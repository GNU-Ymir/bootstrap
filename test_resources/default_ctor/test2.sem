frame: test2::Foo::self (mut self: mut &(mut test2::Foo), x: i32, y: i32, next: (dg ()-> void)[nullable])-> void {
    {
        {
            self.x = x;
            self.y = y;
            self.z = 98
        };
        if (next.func != null) (next.func)(next.closure)
    };
    <unit-value>
}
frame: test2::main ()-> void {
    let a: &(test2::Foo) = test2::Foo::self(new, 1, 2);
    <unit-value>
}
