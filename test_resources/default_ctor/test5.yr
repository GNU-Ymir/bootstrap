// A record and an entity ask for the generated ctor the same way a class does.
record Foo {
   pub let x: i32;
   pub self;
}

entity Bar {
   pub let y: i32 = 0;
   pub self;
}

fn main () {
    let a = Foo (1);
    let b = Bar (y-> 2);
    a.x; b.y;
}
