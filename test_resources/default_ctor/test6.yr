// A ctor written by hand wins over the generated one when both have the same
// signature.
class Foo {
   pub let x: i32;

   pub self (x: i32) with x = x + 1 {}
   pub self;
}

fn main () {
    let a = copy Foo (1);
    a.x;
}
