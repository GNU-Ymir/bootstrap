// A field that is not public is not a parameter of the generated ctor, it is
// always constructed from the value it declares.
class Foo {
   pub {
     let x: i32;
     let mut y: i32;
   }

   let mut z: i32 = 98;

   pub self;
}

fn main () {
    let a = copy Foo (1, 2);
    a.x;
}
