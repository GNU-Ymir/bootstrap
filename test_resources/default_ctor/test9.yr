// An entity gets no ctor it did not ask for, so `self;` generates its only one,
// callable with no argument through the default value of the field parameter.
entity Bar {
   pub let y: i32 = 0;
   pub self;
}

fn main () {
    let b = Bar ();
    b.y;
}
