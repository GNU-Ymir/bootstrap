// An entity already carries the empty ctor that makes it movable, the generated
// one must not collide with it.
entity Bar {
   pub let y: i32 = 0;
   pub self;
}

fn main () {
    let b = Bar ();
    b.y;
}
