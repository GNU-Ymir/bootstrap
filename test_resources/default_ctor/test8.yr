// The generated ctor carries the protection of the `self;` asking for it.
class Foo {
   pub let x: i32;
   prv self;

   pub fn get (self)-> i32 { self.x }
}

fn main () {
    let a = copy Foo (1);
    a.get ();
}
