// A field that is not public and declares no value cannot be constructed, the
// generated ctor has nothing to set it from.
class Foo {
   pub let x: i32;
   let mut z: i32;

   pub self;
}

fn main () {
    let a = copy Foo (1);
    a.x;
}
