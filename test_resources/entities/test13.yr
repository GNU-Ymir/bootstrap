// A no_move ctor cannot reset a moved value.

entity Foo {
    pub let x : i32;

    @no_move
    pub self ()
        with x = 0
    {}
}

fn main () {
    let mut a = Foo ();
    let mut _b_ = move a;
}
