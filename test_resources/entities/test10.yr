// An entity declaring no empty ctor cannot be moved without arguments.

entity Foo {
    pub let x : i32;

    pub self (x : i32)
        with x = x
    {}
}

fn main () {
    let mut a = Foo (12);
    let mut _b_ = move a;
}
