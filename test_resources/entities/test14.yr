// An only_move ctor cannot be called outside of a move.

entity Foo {
    pub let x : i32;

    @only_move
    pub self (x : i32)
        with x = x
    {}
}

fn main () {
    let mut _a_ = Foo (1);
}
