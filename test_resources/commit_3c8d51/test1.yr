fn foo ()-> bool {
    true
}

class X {
    pub {} // empty block to force the template read to fail
           // because class X {if} {} considere the if to be a value which is true
    
    cte if let x = foo () {
        
    }    
} 
