mod syntax::expression::break_;

import syntax::declaration::_;
import syntax::expression::_;

import syntax::keys, syntax::word;
import syntax::tokens;
import std::io;
import utils::stream;

/**
 * Syntaxic node representation of a break expression
 * @verbatim: 
 * ==============
 * break_expr := Keys::BREAK expression:(0)
 * ==============
 */
pub class @final Break over Expression {

    let _value : &Expression;

    /**
     * @params : 
     *    - loc: the location of the break
     *    - value: the value of the break
     */
    pub self (loc : &Word, value : &Expression)
        with super (loc), _value = value
    {}

    /**
     * @returns: the value of the break
     */
    pub def getValue (self)-> &Expression {
        self._value
    }
    
    impl utils::stream::Streamable {

        /**
         * Pretty formatting of the break
         */
        pub over toStream (self, dmut stream : &StringStream) {
            stream:.write (Keys::BREAK, ' 'c8);
            stream:.entabing ();
            self._value.toStream (alias stream);
            stream:.detabing ();
        }        
    }

    impl std::io::Printable;
    
}
