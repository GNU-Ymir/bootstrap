mod test::_;

import test::syntax;

__version TEST {

    /**
     * This module contains all the unit tests of the internal parts of the compiler
     * Ideally, every features should be tested here
     * If the main part is compiled using the version "test", the compiler runs the tests
     */
    pub def runTests () -> void {
        runSyntaxTests ();
    }

}
