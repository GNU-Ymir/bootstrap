import std::io;
import options::state;
import utils::stream;

class X {
    let i = 0;
    let j = 9;

    pub self () {}

    impl Streamable;
}

struct
| i : [c32] = "test"
| j : bool = false
 -> Z;

extern (C) def bootstrap_parse_file (filename : [c8]) {
    println (filename);
    
    let dmut stream = StringStream::new ();
    stream:.writef ("Test %(B), %(y) %(r)", 12, [X::new (), X::new ()], Z ());
    println (stream);    
}
