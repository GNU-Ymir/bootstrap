# E4051

> cannot perform a direct call of the method <> that is empty

where `<>` stands for a value the diagnostic fills in.

Raised during **validation**, from `ValidateErrorMessage::EMPTY_METHOD_CALL` (`src/ymirc/semantic/validator/errors.yr`).

## What it means

A method with no body was called directly rather than through the virtual
dispatch of an instance. An empty method has no code to jump to.

## Example

`test_resources/diagnostics/test30.yr`:

```ymir
@abstract
class Foo {
    pub self () {}
    pub fn bar (self);
}

class Bar over Foo {
    pub self () {}
    pub over bar (self) { self.super.bar (); }
}

fn main () { let _ = copy Bar (); }
```

```text
Note : when using uniform function call syntax
 --> test_resources/diagnostics/test30.yr:(9,38)
 9  ┃     pub over bar (self) { self.super.bar (); }
    ╋              ---                     ^^^
    ┃                ┗━ note: defined here
    ┃                                        ┗━ error: test30::Bar::bar is a method and must be called from an instance
    ┃
    = when validating test30::Bar::bar -- (test_resources/diagnostics/test30.yr:(9,14))
    = when validating -- (test_resources/diagnostics/test30.yr:(7,7))
    ┗━━━━━━━
Error[E4019] : class &(test30::Foo) has no field named bar
 --> test_resources/diagnostics/test30.yr:(9,38)
 9  ┃     pub over bar (self) { self.super.bar (); }
    ╋                                ----- ^^^
    ┃                                    ┗━ note: from an proxied instance of class &(test30::Foo) that is abstract
    ┃                                        ┗━ note: class &(test30::Foo) has no field access method named bar
 --> test_resources/diagnostics/test30.yr:(4,12)
 4  ┃     pub fn bar (self);
    ╋            ---  error: cannot perform a direct call of the method test30::Foo::bar ()-> void that is empty
    ┃
    = when validating test30::Bar::bar -- (test_resources/diagnostics/test30.yr:(9,14))
    = when validating -- (test_resources/diagnostics/test30.yr:(7,7))
    ┗━━━━━━━
```

## How to fix it

Call it on an instance whose dynamic type provides a body, or give the method
a body.

---

[Error code index](README.md)
