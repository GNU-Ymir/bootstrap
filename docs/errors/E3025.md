# E3025

> custom attribute <> may only be applied to classes

where `<>` stands for a value the diagnostic fills in.

Raised during **declaration**, from `DeclareErrorMessage::UNDEFINED_ATTRIBUTE_NO_CLASS` (`src/ymirc/semantic/declarator/errors.yr`).

## What it means

The attribute may only be applied to classes, and it was written on something
else.

## Example

`test_resources/diagnostics/test14.yr`:

```ymir
@abstract
record Foo {
    let x: i32;
}

fn main () { let _ = Foo (1); }
```

```text
Error[E3025] : custom attribute abstract may only be applied to classes
 --> test_resources/diagnostics/test14.yr:(1,2)
 1  ┃ @abstract
    ╋  ^^^^^^^^
    ┃ Note :
    ┃  --> test_resources/diagnostics/test14.yr:(2,1)
    ┃  2  ┃ record Foo {
    ┃     ╋ ^^^^^^
    ┃     ┗━━━━━━━
    ┗━━━━━┻━
```

## How to fix it

Move the attribute onto the class it was meant for, or remove it.

---

[Error code index](README.md)
