# E4307

> union <> declares the member <> twice

where `<>` stands for a value the diagnostic fills in.

Raised during **validation**, from `ValidateErrorMessage::UNION_DUPLICATE_MEMBER` (`src/ymirc/semantic/validator/errors.yr`).

## What it means

A member is identified at runtime by its position in the member list, so two
members naming the same type are two names for one case: nothing can ever
produce the second, and the second of two arms matching it is unreachable. The
duplicate is reported rather than dropped, the note pointing at the first
occurrence.

## Example

`test_resources/type_union/test7.yr`:

```ymir
union U
| A
| B
| A;
```

```text
Error[E4307] : union test7::U declares the member test7::A twice
 --> test_resources/type_union/test7.yr:(14,3)
12  ┃ | A
    ╋   -
13  ┃ | B
14  ┃ | A;
    ╋   ^
    ┃
    = when validating test7::U -- (test_resources/type_union/test7.yr:(11,7))
    ┗━━━━━━━
```

## How to fix it

Remove the repeated member.

---

[Error code index](README.md)
