# E4224

> undefined operator <> for types <> and <>

where `<>` stands for a value the diagnostic fills in.

Raised during **validation**, from `ValidateErrorMessage::UNDEFINED_BIN_OP` (`src/ymirc/semantic/validator/errors.yr`).

## What it means

A binary operator is not defined for the two operand types. Both are in the
message.

## Example

`test_resources/lit_class/operators/test6.yr`:

```ymir
class A {
    pub self () {}
}

fn main () {
    let a = copy A ();
    a * a;

}
```

```text
Error[E4224] : undefined operator * for types &(test6::A) and &(test6::A)
 --> test_resources/lit_class/operators/test6.yr:(7,7)
 7  ┃     a * a;
    ╋       ^  error: class &(test6::A) has no method named opBinary
    ┃       ┗━ error: class &(test6::A) has no method named opBinaryRight
    ┃
    = when validating test6::main -- (test_resources/lit_class/operators/test6.yr:(5,4))
    ┗━━━━━━━
```

## How to fix it

Convert one operand, or define the operator on the type. When both types look
the same, the difference is in their mutability, which is part of the type.

---

[Error code index](README.md)
