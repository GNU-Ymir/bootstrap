# E4106

> the identifier <> describes a native type but has no meaning within this context

where `<>` stands for a value the diagnostic fills in.

Raised during **validation**, from `ValidateErrorMessage::IS_NATIVE_TYPE` (`src/ymirc/semantic/validator/errors.yr`).

## What it means

The name of a native type was used where it has no meaning -- as a value, for
instance, rather than as a type.

## Example

`test_resources/diagnostics/test108.yr`:

```ymir
// a native type used as a value
fn main () {
    let _ = i32;
}
```

```text
Error[E3027] : undefined symbol i32
 --> test_resources/diagnostics/test108.yr:(3,13)
 3  ┃     let _ = i32;
    ╋             ^^^
    ┃
    = when validating test108::main -- (test_resources/diagnostics/test108.yr:(2,4))
    ┗━━━━━━━
Error[E4106] : the identifier i32 describes a native type but has no meaning within this context
 --> test_resources/diagnostics/test108.yr:(3,13)
 3  ┃     let _ = i32;
    ╋             ^^^
    ┃
    = when validating test108::main -- (test_resources/diagnostics/test108.yr:(2,4))
    ┗━━━━━━━
```

## How to fix it

Use the identifier where a type is expected, or use a value of that type.

---

[Error code index](README.md)
