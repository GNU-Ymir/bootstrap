# E4058

> external global variable cannot be declared with a value

Raised during **validation**, from `ValidateErrorMessage::EXTERN_GLOBAL_WITH_VALUE` (`src/ymirc/semantic/validator/errors.yr`).

## What it means

An `extern` global variable was given an initial value. `extern` says the
variable is defined elsewhere; the initial value belongs to that definition.

## Example

`test_resources/diagnostics/test102.yr`:

```ymir
// an external global variable given a value
extern (C) static X : i32 = 1;

fn main () {
    let _ = X;
}
```

```text
Error[E4058] : external global variable cannot be declared with a value
 --> test_resources/diagnostics/test102.yr:(2,19)
 2  ┃ extern (C) static X : i32 = 1;
    ╋                   ^         -
    ┃
    = when validating -- (test_resources/diagnostics/test102.yr:(2,19))
    ┗━━━━━━━
Error[E4245] : undefined symbol X
 --> test_resources/diagnostics/test102.yr:(5,13)
 5  ┃     let _ = X;
    ╋             ^  error: symbol test102::X has errors
 --> test_resources/diagnostics/test102.yr:(2,19)
 2  ┃ extern (C) static X : i32 = 1;
    ╋                   -  error: external global variable cannot be declared with a value
    ┃
    = when validating -- (test_resources/diagnostics/test102.yr:(2,19))
    = when validating test102::main -- (test_resources/diagnostics/test102.yr:(4,4))
    ┗━━━━━━━
```

## How to fix it

Remove the value, or remove `extern` if this module is meant to define
it.

---

[Error code index](README.md)
