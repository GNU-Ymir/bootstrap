# E4152

> <> is enclosed, and cannot be used as a lvalue

**Retired.** The message is still raised, but only as a note under
[E4203](E4203.md), which documents it, and it no longer carries a code of its own.

The code is not reused and not reassigned to another diagnostic, so an E4152
printed by an older compiler still means what is quoted above. This page stays
as its record.

It was raised during **validation**, from `ValidateErrorMessage::NOT_A_LVALUE_CLOSURE`.

---

[Error code index](README.md)
