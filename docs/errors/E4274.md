# E4274

> template specialization expected a value not the type <>

**Retired.** The message is still raised, but only as a note under
[E4236](E4236.md), which documents it, and it no longer carries a code of its own.

The code is not reused and not reassigned to another diagnostic, so an E4274
printed by an older compiler still means what is quoted above. This page stays
as its record.

It was raised during **validation**, from `ValidateErrorMessage::USE_AS_VALUE_TEMPLATE`.

---

[Error code index](README.md)
