# E3001

> a class cannot be both abstract and final

Raised during **declaration**, from `DeclareErrorMessage::ABSTRACT_AND_FINAL` (`src/ymirc/semantic/declarator/errors.yr`).

## What it means

A class is declared both `abstract` and `final`. `abstract` means the class
cannot be instantiated and must be derived from; `final` means it cannot be
derived from. Together they describe a class nothing can ever do anything
with.

## Example

`test_resources/diagnostics/test4.yr`:

```ymir
@abstract @final
class Foo {
    pub self () {}
}

fn main () {}
```

```text
Error[E3001] : a class cannot be both abstract and final
 --> test_resources/diagnostics/test4.yr:(1,12)
 1  ┃ @abstract @final
    ╋            ^^^^^
    ┃ Note :
    ┃  --> test_resources/diagnostics/test4.yr:(1,2)
    ┃  1  ┃ @abstract @final
    ┃     ╋  ^^^^^^^^
    ┃     ┗━━━━━━━
    ┃ Note :
    ┃  --> test_resources/diagnostics/test4.yr:(2,1)
    ┃  2  ┃ class Foo {
    ┃     ╋ ^^^^^
    ┃     ┗━━━━━━━
    ┗━━━━━┻━
```

## How to fix it

Drop whichever of the two does not apply.

---

[Error code index](README.md)
