# E4122

> the type <> cannot be used as a key, it is not hashable

where `<>` stands for a value the diagnostic fills in.

Raised during **validation**, from `ValidateErrorMessage::MAP_KEY_NOT_HASHABLE` (`src/ymirc/semantic/validator/errors.yr`).

## What it means

A type is used as a map key and cannot be hashed.

## Example

`test_resources/diagnostics/test112.yr`:

```ymir
// a class used as a map key without being hashable
class Key {
    pub self () {}
}

fn main () {
    let _m_ : [&Key => i32] = copy [copy Key () => 1];
}
```

```text
Error[E4122] : the type &(test112::Key) cannot be used as a key, it is not hashable
 --> test_resources/diagnostics/test112.yr:(2,7)
 2  ┃ class Key {
    ╋       ^^^  error: class type test112::Key does not implement trait core::types::Hashable
 --> test_resources/diagnostics/test112.yr:(7,36)
 7  ┃     let _m_ : [&Key => i32] = copy [copy Key () => 1];
    ╋                                    -
    ┃
    = when validating -- (test_resources/diagnostics/test112.yr:(7,36))
    = when validating test112::main -- (test_resources/diagnostics/test112.yr:(6,4))
    ┗━━━━━━━
```

## How to fix it

Make the type hashable, or key the map on a value derived from it that
already is.

---

[Error code index](README.md)
