# E4219

> temporary type <> has no type information

**Retired.** The message is still raised, but only as a note under
[E4222](E4222.md), which documents it, and it no longer carries a code of its own.

The code is not reused and not reassigned to another diagnostic, so an E4219
printed by an older compiler still means what is quoted above. This page stays
as its record.

It was raised during **validation**, from `ValidateErrorMessage::TYPE_HAS_NO_TYPEINFO`.

---

[Error code index](README.md)
