# E4091

> implicit override of method <> by trait impl

where `<>` stands for a value the diagnostic fills in.

Raised during **validation**, from `ValidateErrorMessage::IMPLICIT_OVERRIDE_BY_TRAIT` (`src/ymirc/semantic/validator/errors.yr`).

## What it means

A trait `impl` provides a method that overrides an ancestor method, without the
override being declared.

## Example

`test_resources/diagnostics/test107.yr`:

```ymir
// a trait implemented by a class whose ancestor already has a method of that name
trait Shape {
    pub fn area (self)-> i32 { 1 }
}

class Base {
    pub self () {}
    pub fn area (self)-> i32 { 2 }
}

class Square over Base {
    pub self () with super () {}
    impl Shape;
}

fn main () {
    let _ = copy Square ();
}
```

```text
Error[E4091] : implicit override of method test107::Base::area by trait impl
 --> test_resources/diagnostics/test107.yr:(13,5)
13  ┃     impl Shape;
    ╋     ^^^^
 --> test_resources/diagnostics/test107.yr:(3,12)
 3  ┃     pub fn area (self)-> i32 { 1 }
    ╋            ----
    ┃
    = when validating -- (test_resources/diagnostics/test107.yr:(11,7))
    ┗━━━━━━━
Error[E4104] : symbol test107::Square has errors
 --> test_resources/diagnostics/test107.yr:(17,18)
17  ┃     let _ = copy Square ();
    ╋                  ^^^^^^
 --> test_resources/diagnostics/test107.yr:(3,12)
 3  ┃     pub fn area (self)-> i32 { 1 }
    ╋            ----
   ...
13  ┃     impl Shape;
    ╋     ----  error: implicit override of method test107::Base::area by trait impl
    ┃
    = when validating -- (test_resources/diagnostics/test107.yr:(11,7))
    = when validating test107::main -- (test_resources/diagnostics/test107.yr:(16,4))
    ┗━━━━━━━
```

## How to fix it

Mark the method `override` in the `impl`, or rename it.

---

[Error code index](README.md)
