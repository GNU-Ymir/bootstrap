# E4069

> the type cannot be validated due to forward reference

Raised during **validation**, from `ValidateErrorMessage::FORWARD_REFERENCE_TYPE` (`src/ymirc/semantic/validator/errors.yr`).

## What it means

A type refers to itself, or to a type still being validated, through a path the
compiler must resolve first.

## Example

`test_resources/diagnostics/test135.yr`:

```ymir
// two globals whose types depend on each other
lazy X : i32 = Y;
lazy Y : i32 = X;

fn main () {
    let _ = X;
}
```

```text
Error[E4245] : undefined symbol Y
 --> test_resources/diagnostics/test135.yr:(2,16)
 2  ┃ lazy X : i32 = Y;
    ╋      -         ^
    ┃      ┗━ error: the type cannot be validated due to forward reference
    ┃                ┗━ error: symbol test135::Y has errors
 3  ┃ lazy Y : i32 = X;
    ╋                -  error: undefined symbol X
    ┃                ┗━ error: symbol test135::X has errors
    ┃
    = when validating -- (test_resources/diagnostics/test135.yr:(3,6))
    = when validating -- (test_resources/diagnostics/test135.yr:(2,6))
    ┗━━━━━━━
Error[E4245] : undefined symbol X
 --> test_resources/diagnostics/test135.yr:(3,16)
 2  ┃ lazy X : i32 = Y;
    ╋      -  error: the type cannot be validated due to forward reference
 3  ┃ lazy Y : i32 = X;
    ╋                ^  error: symbol test135::X has errors
    ┃
    = when validating -- (test_resources/diagnostics/test135.yr:(3,6))
    ┗━━━━━━━
Error[E4245] : undefined symbol X
 --> test_resources/diagnostics/test135.yr:(6,13)
 6  ┃     let _ = X;
    ╋             ^  error: symbol test135::X has errors
 --> test_resources/diagnostics/test135.yr:(2,6)
 2  ┃ lazy X : i32 = Y;
    ╋      -         -
    ┃      ┗━ error: the type cannot be validated due to forward reference
    ┃                ┣━ error: undefined symbol Y
    ┃                ┗━ error: symbol test135::Y has errors
 3  ┃ lazy Y : i32 = X;
    ╋                -  error: undefined symbol X
    ┃                ┗━ error: symbol test135::X has errors
    ┃
    = when validating -- (test_resources/diagnostics/test135.yr:(3,6))
    = when validating -- (test_resources/diagnostics/test135.yr:(2,6))
    = when validating test135::main -- (test_resources/diagnostics/test135.yr:(5,4))
    ┗━━━━━━━
```

## How to fix it

Break the cycle by holding the self-reference behind a class or a pointer, cf.
[E4017](E4017.md).

---

[Error code index](README.md)
