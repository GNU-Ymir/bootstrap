mod main;
import std::io;

class X {

    pub def createSyntaxVarFromPath (self, loc : &Word, path : [[c8]])-> &Expression {
        let x = hmap #[
            1 => 12, 23 => 34
        ];
       
        a;
    }
}

def main () {
    let a = (1, 2, (3,4));
    for i in a {
        //i;
        cte if !is!typeof (i) {tuple U} {
            break;
        } else {
            //print (i);
        }
    }
}
