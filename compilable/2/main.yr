mod main;
import std::io;


def main () {
    let a = (1, 2, (3,4));
    for i in a {
        //i;
        cte if !is!typeof (i) {tuple U} {
            break;
        } else {
            //print (i);
        }
    }
}
