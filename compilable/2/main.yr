import std::io;


def bar {T...} (a : T) {
    let b = 187;
    let d = copy |x| { // gymir commit 800 (move inside template where forgotten)
        println ((a,), b, x);// (a,) instead of just a, to make sure is it is considered a tuple by println func
    };

    d (12);
}

def main () {
    bar (1, 2, 3, 4);
}
