mod main;
import std::io;

def foo {T...} (a : T) {
    cte if (is!T {tuple U}) {
        for i in a {
            print (i);
        }
    } else {
        print (a);
    }
}

def main () {
    foo (1);
    foo (1, 'r');
    foo (1, 2, 3);
}
