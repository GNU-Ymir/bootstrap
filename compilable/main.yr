import std::io;

def main ()
   // throws &AssertError
{
    // assert (("Hello World!!").startWith ("Hello"));
    // assert (("Hello World!!").endWith ("World!!"));

    // assert (!("Hello World!!").startWith ("ello"));
    // assert (!("Hello World!!").endWith ("World!"));

    let dmut a = [1, 2, 3];
    println (a [0], a [1]);
    
}
