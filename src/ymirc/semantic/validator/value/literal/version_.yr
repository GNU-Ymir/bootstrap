in version_;

use ymirc::syntax::expression::_;
use ymirc::errors::_;
use ymirc::semantic::generator::{value::_, type::_};

use ymirc::lexing::keys;

pub mod VersionLiteralValidation {

    pub fn validate(dmut context: &ValidationContext, v: &VersionExpr)-> &Value
        throws ErrorMsg
    {
        context;
        v;
        // TODO
        panic;
    }

}
