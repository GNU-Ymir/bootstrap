in mult;

use ymirc::syntax::expression::operator;
use ymirc::semantic::validator;
use ymirc::semantic::generator::value;

use ymirc::errors::message;

pub mod MultValidation {

    pub fn validate(dmut context: &ValidationContext, op: &MultOperatorExpr)-> &Value
        throws ErrorMsg
    {
        context;
        op;

        panic;
    }

}
