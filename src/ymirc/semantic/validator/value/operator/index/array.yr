in array;

use ymirc::syntax::expression::operator;
use ymirc::semantic::validator;
use ymirc::semantic::generator::{value, type};

use ymirc::errors::message;
use ymirc::lexing::word;


pub mod ArrayIndexValidation {

    pub fn validate (dmut context: &ValidationContext, loc: &Word, type: &Type, value: &Value, index: &Value)-> &Value
        throws ErrorMsg
    {
        context;
        type;
        value;
        index;

        panic;
    }
    
}
