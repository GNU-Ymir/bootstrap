in macro_;

use ymirc::syntax::expression::operator;
use ymirc::semantic::validator;
use ymirc::semantic::generator::value;

use ymirc::errors::message;

pub mod MacroValidation {

    pub fn validate(dmut context: &ValidationContext, op: &MacroCallExpr)-> &Value
        throws ErrorMsg
    {
        context;
        op;

        panic;
    }

}
