in binary;

use ymirc::syntax::expression::operator;
use ymirc::semantic::validator;
use ymirc::semantic::generator::value;

use ymirc::errors::message;

pub mod BinaryValidation {

    pub fn validate (dmut context: &ValidationContext, op: &BinaryExpr)-> &Value
        throws ErrorMsg
    {
        context;
        op;
        
        panic;
    }
    
}
