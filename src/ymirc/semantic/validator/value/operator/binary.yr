in binary;

pub mod ::class_;
pub mod ::range;

use ymirc::lexing::{word, tokens, keys};

use ymirc::syntax::expression::operator;
use ymirc::semantic::validator;
use ymirc::semantic::generator::value;

use ymirc::errors::message;

pub mod BinaryValidation {

    pub fn validate (dmut context: &ValidationContext, op: &BinaryExpr)-> &Value
        throws ErrorMsg
    {
        context;
        op;
        
        panic;
    }


    pub fn validateComparison (dmut context: &ValidationContext, loc: &Word, op: BinaryOperators, left: &Value, right: &Value)-> &Value
        throws ErrorMsg
    {
        panic;
    }
    
}
