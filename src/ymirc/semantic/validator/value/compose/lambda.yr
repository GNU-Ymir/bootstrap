in lambda;

use ymirc::syntax::expression::_;
use ymirc::errors::_;
use ymirc::semantic::generator::{value::_, type::_};

use ymirc::lexing::{keys, word};

pub mod LambdaComposeValidation {

    pub fn validate(dmut context: &ValidationContext, v: &LambdaExpr)-> &Value
        throws ErrorMsg
    {
        context;
        v;
        // TODO
        panic;
    }

    pub fn validateLazy(dmut context: &ValidationContext, lazyLoc: &Word, v: &LambdaExpr)-> &Value
        throws ErrorMsg
    {
        context;
        v;
        // TODO
        panic;
    }

    pub fn validateClosure(dmut context: &ValidationContext, copyLoc: &Word, v: &LambdaExpr)-> &Value
        throws ErrorMsg
    {
        context;
        copyLoc;
        v;
        // TODO
        panic;
    }

    pub fn validateWrap(dmut context: &ValidationContext, v: &LambdaValueWrapperExpr)-> &Value
        throws ErrorMsg
    {
        context;
        v;
        // TODO
        panic;
    }

}
