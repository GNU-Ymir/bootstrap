in arrayfor;

use ymirc::syntax::expression::_;
use ymirc::errors::_;
use ymirc::semantic::generator::{value::_, type::_};

use ymirc::lexing::keys;

pub mod ArrayForComposeValidation {

    pub fn validate (dmut context: &ValidationContext, v: &SliceForAllocatorExpr)-> &Value
        throws ErrorMsg
    {
        context;
        v;
        // TODO
        panic;
    }
    
}
