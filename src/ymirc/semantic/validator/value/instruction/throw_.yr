in throw_;

use ymirc::syntax::expression::instruction;
use ymirc::semantic::validator;
use ymirc::semantic::generator::value;
use ymirc::errors::message;

pub mod ThrowValidation {

    pub fn validate (dmut context: &ValidationContext, la: &ThrowExpr)-> &Value
        throws ErrorMsg
    {        
        context;
        la;
        panic;
    }
    
}
