in panic_;

use ymirc::syntax::expression::instruction;
use ymirc::semantic::validator;
use ymirc::semantic::generator::value;
use ymirc::errors::message;

pub mod PanicValidation {

    pub fn validate (dmut context: &ValidationContext, la: &PanicExpr)-> &Value
        throws ErrorMsg
    {        
        context;
        la;
        panic;
    }
    
}
