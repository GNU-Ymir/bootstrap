in ctor;

use ymirc::semantic::{validator, template_};
use ymirc::semantic::{generator::_, symbol};
use ymirc::semantic::generator::{value::_, type::_};

use ymirc::errors::_;
use ymirc::utils::{format, log};
use ymirc::lexing::{word, tokens, keys};

use std::{stream, io};

pub mod CtorValidation {
    
    pub fn validateProto (dmut context: &ValidationContext, dmut sym: &CtorSymbol, cref: &ClassRefType)-> &CtorPrototypeValue
        throws ErrorMsg
    {
        context;
        sym;
        panic;
    }
    
}
