in custom;

pub mod ::classptr;
pub mod ::enum_;

use ymirc::lexing::word;

/**
 * A custom type is type defined by the user
 */
@abstract
pub class CustomType over Type {
    pub self (loc : &Word, isMutable : bool = false)
        with super (loc, isMutable-> isMutable)
    {}
}
