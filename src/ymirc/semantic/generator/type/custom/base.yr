mod ymirc::semantic::generator::type::custom::base;

import ymirc::semantic::generator::type::_;
import ymirc::lexing::word;

/**
 * A custom type is type defined by the user
 */
pub class @abstract CustomType over Type {

    /**
     * 
     */
    pub self (loc : &Word, isMutable : bool = false)
        with super (loc, isMutable-> isMutable)
    {}
        
}
