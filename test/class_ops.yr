in class_ops;

use ymirc::utils::_;
use utils;

use std::io;

// Test for operators on classes
__test {
    for i in 1 ... 22 {
        logging::setRelativeTime ();
        logging::info (" Test lit class operators % start", i);
        utils::registerTest ("test_resources/lit_class/operators/test" ~ std::conv::to!{[c8]} (i) ~ ".yr");
        logging::success (" Test lit class operators % finished", i);
    }
}
