in lit_map;

use ymirc::utils::_;
use utils;

__test {
    for i in 1 ... 10 {
        logging::setRelativeTime ();
        logging::info (" Test lit map % start", i);
        utils::registerTest ("test_resources/lit_map/test" ~ std::conv::to!{[c8]} (i) ~ ".yr");
        logging::success (" Test lit map % finished", i);
    }
}
