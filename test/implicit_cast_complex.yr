in implicit_cast_complex;

use ymirc::utils::_;
use utils;

use std::io;

// Test for implicit cast
__test {
    for i in 1 ... 7 {
        logging::setRelativeTime ();
        logging::info (" Test implicit cast complex % start", i);
        utils::registerTest ("test_resources/implicit_cast/complex/test" ~ std::conv::to!{[c8]} (i) ~ ".yr");
        logging::success (" Test implicit cast complex % finished", i);
    }
}
