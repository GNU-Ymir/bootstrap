in scope_guards;

use ymirc::utils::_;
use utils;

use std::io;


__test {
    for i in 1 ... 9 {
        logging::setRelativeTime ();
        logging::info ("Test scope guards % start", i);
        utils::registerTest ("test_resources/scope_guards/test" ~ std::conv::to!{[c8]} (i) ~ ".yr");
        logging::success ("Test scope guards % finished", i);
    }
}
