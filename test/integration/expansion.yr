in expansion;

use ymirc::utils::_;
use utils;


__test {
    for i in 1 ... 7 {
        logging::setRelativeTime ();
        logging::info (" Test expansion % start", i);
        utils::registerTest ("test_resources/expansion/test" ~ std::conv::to!{[c8]} (i) ~ ".yr");
        logging::success (" Test expansion % finished", i);
    }
}
