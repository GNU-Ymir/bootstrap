in unions;

use ymirc::utils::_;
use utils;

__test {
    for i in 1 ... 9 {
        logging::setRelativeTime ();
        logging::info (" Test unions % start", i);
        utils::registerTest ("test_resources/union/test" ~ std::conv::to!{[c8]} (i) ~ ".yr");
        logging::success (" Test unions % finished", i);
    }
}
