in function;

use ymirc::utils::_;
use utils;

use std::io;

__test {
    for i in 1 ... 16 {
        logging::setRelativeTime ();
        logging::info ("Test function % start", i);
        utils::registerTest ("test_resources/function/test" ~ std::conv::to!{[c8]} (i) ~ ".yr");
        logging::success ("Test function % finished", i);
    }
}
