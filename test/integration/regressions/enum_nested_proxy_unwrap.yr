in enum_nested_proxy_unwrap;

// 8c5975 => fix: infinite loop in EnumType::getLastLevelProxy when unwrapping nested enums
@parameters(copy [i for i in 1 ... 1])
__test nested_enum_proxy_unwrapping (i: i32) {
    utils::registerCase ("test_resources/regressions/enum_nested_proxy_unwrap", i);
}
