in typeof_path_precedence;

use std::io;
use ymirc::utils::_;
use utils;

// feat: 'typeof (X)' is a level 4 operand, so 'typeof (X)::field.len' is allowed
__test {
    logging::setRelativeTime ();
    logging::info (" Test regression typeof_path_precedence 1 start");
    utils::registerTest ("test_resources/regressions/typeof_path_precedence/test1.yr");
    logging::success (" Test regression typeof_path_precedence 1 finished");
}
