in native_compound_type_compat;

//  fix: fix type compability of native compound types
@parameters(copy [i for i in 1 ... 2])
__test native_compound_type_compatibility (i: i32) {
    utils::registerCase ("test_resources/regressions/native_compound_type_compat", i);
}
