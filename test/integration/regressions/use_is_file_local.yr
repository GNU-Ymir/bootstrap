in use_is_file_local;

use ymirc::utils::_;
use utils;

// a3993 => fix: fix use of module is only local to a file
__test use_is_local_to_a_file {
    utils::registerTests ("test_resources/regressions/use_is_file_local", 1, 3, suffix-> "/foo.yr");
}
