in assert_bool_type;

use ymirc::utils::_;
use utils;

// 9b4cdc1 => fix assert test bool type test....
__test {
    for i in 1 ... 5 {
        logging::setRelativeTime ();
        logging::info (" Test regression assert_bool_type % start", i);
        utils::registerTest ("test_resources/regressions/assert_bool_type/test" ~ std::conv::to!{[c8]} (i) ~ ".yr");
        logging::success (" Test regression assert_bool_type % finished", i);
    }
}
