in record_field_and_ctor_redirect;

// c7d8dde => fix: nested record field assignment through an index, and ctor redirect to 'self ()'
@parameters(utils::range (1, 2))
__test nested_field_assignment_and_ctor_redirect (i: i32) {
    utils::registerCase ("test_resources/regressions/record_field_and_ctor_redirect", i);
}
