in float_properties;

// b1185 => feat: floating point properties
@parameters(copy [i for i in 1 ... 2])
__test float_type_properties (i: i32) {
    utils::registerCase ("test_resources/regressions/float_properties", i);
}
