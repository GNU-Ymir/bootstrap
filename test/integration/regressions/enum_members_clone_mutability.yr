in enum_members_clone_mutability;

// a30621 => fix: fix clone mutability level
@parameters(copy [i for i in 1 ... 1])
__test enum_member_clone_mutability (i: i32) {
    utils::registerCase ("test_resources/regressions/enum_members_clone_mutability", i);
}
