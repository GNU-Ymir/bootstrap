in enum_members_clone_mutability;

use ymirc::utils::_;
use utils;

// a30621 => fix: fix clone mutability level
__test {
    for i in 1 ... 1 {
        logging::setRelativeTime ();
        logging::info (" Test regression enum_members_clone_mutability % start", i);
        utils::registerTest ("test_resources/regressions/enum_members_clone_mutability/test" ~ std::conv::to!{[c8]} (i) ~ ".yr");
        logging::success (" Test regression enum_members_clone_mutability % finished", i);
    }
}
