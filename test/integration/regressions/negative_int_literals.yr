in negative_int_literals;

// fix: the minimum of a signed type could not be written as a literal
@parameters(utils::range (1, 3))
__test minimum_of_a_signed_type (i: i32) {
    utils::registerCase ("test_resources/regressions/negative_int_literals", i);
}
