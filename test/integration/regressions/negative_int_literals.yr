in negative_int_literals;

use ymirc::utils::_;
use utils;

// fix: the minimum of a signed type could not be written as a literal
__test {
    utils::registerTests ("test_resources/regressions/negative_int_literals", 1, 3);
}
