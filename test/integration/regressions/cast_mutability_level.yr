in cast_mutability_level;

use ymirc::utils::_;
use utils;

// 02355 => fix: fix clone mutability level
__test {
    for i in 1 ... 3 {
        logging::setRelativeTime ();
        logging::info (" Test regression cast_mutability_level % start", i);
        utils::registerTest ("test_resources/regressions/cast_mutability_level/test" ~ std::conv::to!{[c8]} (i) ~ ".yr");
        logging::success (" Test regression cast_mutability_level % finished", i);
    }
}
