in module_import_errors;

// 7e73ed => fix: recursive imports, and child global modules imported outside a global module
@parameters(utils::range (1, 2))
__test recursive_and_global_imports (i: i32) {
    utils::registerCase ("test_resources/regressions/module_import_errors", i);
}
