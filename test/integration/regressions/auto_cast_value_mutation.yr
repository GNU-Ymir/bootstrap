in auto_cast_value_mutation;

use ymirc::utils::_;
use utils;

// 8bdc49 => fix: auto casting can change value during compatibility verification
__test auto_cast_keeps_the_value {
    utils::registerTests ("test_resources/regressions/auto_cast_value_mutation", 1, 2);
}
