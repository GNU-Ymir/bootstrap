in auto_cast_value_mutation;

// 8bdc49 => fix: auto casting can change value during compatibility verification
@parameters(utils::range (1, 2))
__test auto_cast_keeps_the_value (i: i32) {
    utils::registerCase ("test_resources/regressions/auto_cast_value_mutation", i);
}
