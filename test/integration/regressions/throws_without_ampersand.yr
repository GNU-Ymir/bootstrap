in throws_without_ampersand;

use ymirc::utils::_;
use utils;

// b2ebe => change: change throws &X to throws X, as the & was useless
__test throws_without_an_ampersand {
    utils::registerTests ("test_resources/regressions/throws_without_ampersand", 1, 3);
}
