in templates;

use ymirc::utils::_;
use utils;

__test {
    for i in 1 ... 21 {
        logging::setRelativeTime ();
        logging::info (" Test template % start", i);
        utils::registerTest ("test_resources/templates/test" ~ std::conv::to!{[c8]} (i) ~ ".yr");
        logging::success (" Test template ", i, " end");
    }
}
