in implement;

use ymirc::utils::_;
use utils;

use std::io;

// Test implementations of traits
__test {
    for i in 1 ... 12 {
        logging::setRelativeTime ();
        logging::info (" Test lit class operators % start", i);
        utils::registerTest ("test_resources/implement/test" ~ std::conv::to!{[c8]} (i) ~ ".yr");
        logging::success (" Test lit class operators % finished", i);
    }
}
