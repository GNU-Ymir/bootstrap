in for_loops;

use ymirc::utils::_;
use utils;

mod arrays {
    __test {
        for i in 1 ... 19 {
            logging::setRelativeTime ();
            logging::info (" Test for loops on arrays % start", i);
            utils::registerTest ("test_resources/for_loops/arrays/test" ~ std::conv::to!{[c8]} (i) ~ ".yr");
            logging::success (" Test for loops on arrays % finished", i);
        }
    }
}

/**
 * ======================================================================
 * ======================================================================
 * ==========================      SLICES     ===========================
 * ======================================================================
 * ======================================================================
 */

mod slices {
    __test {
        for i in 1 ... 21 {
            logging::setRelativeTime ();
            logging::info (" Test for loops on slices % start", i);
            utils::registerTest ("test_resources/for_loops/slices/test" ~ std::conv::to!{[c8]} (i) ~ ".yr");
            logging::success (" Test for loops on slices % finished", i);
        }
    }
}

/**
 * ======================================================================
 * ======================================================================
 * ==========================      TUPLES     ===========================
 * ======================================================================
 * ======================================================================
 */

mod tuples {
    __test {
        for i in 1 ... 10 {
            logging::setRelativeTime ();
            logging::info (" Test for loops on tuples % start", i);
            utils::registerTest ("test_resources/for_loops/tuples/test" ~ std::conv::to!{[c8]} (i) ~ ".yr");
            logging::success (" Test for loops on tuples % finished", i);
        }
    }
}

/*!
 * ====================================================================================================
 * ====================================================================================================
 * ===================================          LST COMPR          ====================================
 * ====================================================================================================
 * ====================================================================================================
 */

mod lst_compr {
    __test {
        for i in 1 ... 22 {
            logging::setRelativeTime ();
            logging::info (" Test list compr % start", i);
            utils::registerTest ("test_resources/for_loops/lst_compr/test" ~ std::conv::to!{[c8]} (i) ~ ".yr");
            logging::success (" Test for loops on arrays % finished", i);
        }
    }
}

/*!
 * ====================================================================================================
 * ====================================================================================================
 * ====================================          OVERRIDE          ====================================
 * ====================================================================================================
 * ====================================================================================================
 */


mod override {
    __test {
        for i in 1 ... 12 {
            logging::setRelativeTime ();
            logging::info (" Test override % start", i);
            utils::registerTest ("test_resources/for_loops/override/test" ~ std::conv::to!{[c8]} (i) ~ ".yr");
            logging::success (" Test override % finished", i);
        }
    }
}

/*!
 * ====================================================================================================
 * ====================================================================================================
 * =================================          OVERRIDE COMPR          =================================
 * ====================================================================================================
 * ====================================================================================================
 */

mod override_compr {
    __test {
        for i in 1 ... 12 {
            logging::setRelativeTime ();
            logging::info (" Test lst_compr_class % start", i);
            utils::registerTest ("test_resources/for_loops/lst_compr_class/test" ~ std::conv::to!{[c8]} (i) ~ ".yr");
            logging::success (" Test lst_compr_class % finished", i);
        }
    }
}
