in commit_a3993;

use ymirc::utils::_;
use utils;


// a3993 => fix: fix use of module is only local to a file
__test {
    for i in 1 ... 3 {
        logging::setRelativeTime ();
        logging::info (" Test commit a3993 % start", i);
        utils::registerTest ("test_resources/commit_a3993/test" ~ std::conv::to!{[c8]} (i) ~ "/foo.yr");
        logging::success (" Test commit a3993 % finished", i);
    }
}
