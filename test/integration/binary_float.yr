in binary_float;

use ymirc::utils::_;
use utils;
use std::io;

// Test for binary operators on floats
__test {
    for i in 1 ... 7 {
        logging::setRelativeTime ();
        logging::info (" Test binary float % start", i);
        utils::registerTest ("test_resources/binary_float/test" ~ std::conv::to!{[c8]} (i) ~ ".yr");
        logging::success (" Test binary float % finished", i);
    }
}
