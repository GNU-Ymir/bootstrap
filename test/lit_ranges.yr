in lit_ranges;

use ymirc::utils::_;
use utils;

__test {
    for i in 1 ... 9 {
        logging::setRelativeTime ();
        logging::info (" Test lit ranges ", i, " start");
        utils::registerTest ("test_resources/lit_ranges/test" ~ std::conv::to!{[c8]} (i) ~ ".yr");
        logging::success (" Test lit ranges ", i, " finished");
    }
}
