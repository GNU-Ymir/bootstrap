in char_type;

use ymirc::utils::_;
use utils;

use std::io;

// Test for char type
__test {
    for i in 1 ... 6 {
        logging::setRelativeTime ();
        logging::info (" Test char type ", i, " start");
        utils::registerTest ("test_resources/char_type/test" ~ std::conv::to!{[c8]} (i) ~ ".yr");
        logging::success (" Test char type ", i, " finished");
    }
}
