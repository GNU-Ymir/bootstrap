in lit_tuples;

use ymirc::utils::_;
use utils;


__test {
    for i in 1 ... 15 {
        logging::setRelativeTime ();
        logging::info (" Test lit tuples ", i, " start");
        utils::registerTest ("test_resources/lit_tuples/test" ~ std::conv::to!{[c8]} (i) ~ ".yr");
        logging::success (" Test lit tuples ", i, " finished");
    }
}
