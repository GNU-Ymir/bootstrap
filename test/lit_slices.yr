in lit_slices;

use ymirc::utils::_;
use utils;

__test {
    for i in 1 ... 22 {
        logging::setRelativeTime ();
        logging::info (" Test lit slices ", i, " start");
        utils::registerTest ("test_resources/lit_slices/test" ~ std::conv::to!{[c8]} (i) ~ ".yr");
        logging::success (" Test lit slices ", i, " finished");
    }
}
