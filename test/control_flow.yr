in control_flow;

use ymirc::utils::_;
use utils;

use std::io;


__test {
    for i in 1 ... 26 {
        logging::setRelativeTime ();
        logging::info ("Test control flow % start", i);
        utils::registerTest ("test_resources/control_flow/test" ~ std::conv::to!{[c8]} (i) ~ ".yr");
        logging::success ("Test control flow 1 finished");
    }
}
