in cast_float;

use ymirc::utils::_;
use utils;

use std::io;

// Test for casting on float to floats
__test {
    for i in 1 ... 3 {
        logging::setRelativeTime ();
        logging::info (" Test cast float % start", i);
        utils::registerTest ("test_resources/cast_float/test" ~ std::conv::to!{[c8]} (i) ~ ".yr");
        logging::success (" Test cast float % finished", i);
    }
}
