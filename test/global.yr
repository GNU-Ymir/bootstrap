in global;

use ymirc::utils::_;
use utils;


__test {
    for i in 1 ... 10 {
        logging::setRelativeTime ();
        logging::info (" Test global ", i, " start");
        utils::registerTest ("test_resources/global/test" ~ std::conv::to!{[c8]} (i) ~ ".yr");
        logging::success (" Test global ", i, " finished");
    }
}
