in implicit_cast;

use ymirc::utils::_;
use utils;

use std::io;

// Test for implicit cast
__test {
    for i in 1 ... 10 {
        logging::setRelativeTime ();
        logging::info (" Test implicit cast % start", i);
        utils::registerTest ("test_resources/implicit_cast/test" ~ std::conv::to!{[c8]} (i) ~ ".yr");
        logging::success (" Test implicit cast % finished", i);
    }
}

