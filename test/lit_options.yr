in lit_options;

use ymirc::utils::_;
use utils;


__test {
    for i in 1 ... 18 {
        logging::setRelativeTime ();
        logging::info (" Test lit options ", i, " start");
        utils::registerTest ("test_resources/lit_options/test" ~ std::conv::to!{[c8]} (i) ~ ".yr");
        logging::success (" Test lit options ", i, " finished");
    }
}
