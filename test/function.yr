in function;

use ymirc::utils::_;
use utils;

use std::io;

__test {
    for i in 1 ... 16 {
        logging::setRelativeTime ();
        logging::info ("Test function ", i, " start");
        utils::registerTest ("test_resources/function/test" ~ std::conv::to!{[c8]} (i) ~ ".yr");
        logging::success ("Test function ", i, " finished");
    }
}
