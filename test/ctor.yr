in ctor;

use ymirc::utils::_;
use utils;

use std::io;

// Test for binary operators on floats
__test {
    logging::setRelativeTime ();
    logging::info (" Test ctor 1 start");
    utils::registerTest ("test_resources/ctor/test1.yr");
    logging::success (" Test ctor 1 finished");
}
