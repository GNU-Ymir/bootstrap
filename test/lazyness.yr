in lazyness;

use ymirc::utils::_;
use utils;


__test {
    for i in 1 ... 18 {
        logging::setRelativeTime ();
        logging::info (" Test lazyness % start", i);
        utils::registerTest ("test_resources/lazyness/test" ~ std::conv::to!{[c8]} (i) ~ ".yr");
        logging::success (" Test lazyness % finished", i);
    }
}

