in commit_8bdc49;

use ymirc::utils::_;
use utils;

// 8bdc49 => fix: auto casting can change value during compatibility verification
__test {
    for i in 1 ... 2 {
        logging::setRelativeTime ();
        logging::info (" Test commit 8bdc49 % start", i);
        utils::registerTest ("test_resources/commit_8bdc49/test" ~ std::conv::to!{[c8]} (i) ~ ".yr");
        logging::success (" Test commit 8bdc49 % finished", i);
    }
}
