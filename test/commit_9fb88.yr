mod commit_9fb88;

import ymirc::utils::_;
import ymirc::semantic::generator::frame;
import utils;


// 9fb88 => fix: fix compile time interpret
__test {
    logging::setRelativeTime ();
    logging::info (" Test commit 9fb88 1 start"s8);

    let mut found = false;
    let res = utils::compileFile ("test_resources/commit_9fb88/test1.yr"s8);
    utils::ensureErrorEmpty (res.err);
    for g in res.sem {
        match g {
            f : &FrameGenerator => {
                if (f.getName ().toStr () == "test1/main"s8) {
                    found = true;
                    utils::compareResultWithFile ([g], "test_resources/commit_9fb88/test1_out.txt"s8);
                }
            }
        }
    }

    assert (found, "Function main not found ?");
    logging::success (" Test commit 9fb88 1 finished"s8);
}
