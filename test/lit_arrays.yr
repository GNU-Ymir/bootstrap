in lit_arrays;

use ymirc::utils::_;
use utils;

__test {
    for i in 1 ... 19 {
        logging::setRelativeTime ();
        logging::info (" Test lit arrays % start", i);
        utils::registerTest ("test_resources/lit_arrays/test" ~ std::conv::to!{[c8]} (i) ~ ".yr");
        logging::success (" Test lit arrays % finished", i);
    }
}
