in lit_class;

use ymirc::utils::_;
use utils;


__test {
    for i in 1 ... 7 {
        logging::setRelativeTime ();
        logging::info (" Test lit class % start", i);
        utils::registerTest ("test_resources/lit_class/test" ~ std::conv::to!{[c8]} (i) ~ ".yr");
        logging::success (" Test lit class % finished", i);
    }
}

mod errors {
    __test {
        for i in 1 ... 10 {
            logging::setRelativeTime ();
            logging::info (" Test lit class err % start", i);
            utils::registerTest ("test_resources/lit_class/errors/test" ~ std::conv::to!{[c8]} (i) ~ ".yr");
            logging::success (" Test lit class err % finished", i);
        }
    }
}

mod methods {
    __test {
        for i in 1 ... 9 {
            logging::setRelativeTime ();
            logging::info (" Test lit class methods % start", i);
            utils::registerTest ("test_resources/lit_class/methods/test" ~ std::conv::to!{[c8]} (i) ~ ".yr");
            logging::success (" Test lit class methods % finished", i);
        }
    }
}
