in unions;

use ymirc::utils::_;
use utils;

__test {
    for i in 1 ... 7 {
        logging::setRelativeTime ();
        logging::info (" Test unions ", i, " start");
        utils::registerTest ("test_resources/union/test" ~ std::conv::to!{[c8]} (i) ~ ".yr");
        logging::success (" Test unions ", i, " finished");
    }
}
