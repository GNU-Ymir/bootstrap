in commit_44f75;

use ymirc::utils::_;
use utils;

use std::io;

//  fix: moved implicit casting validation to the cast operator validation
__test {
    for i in 1 ... 2 {
        logging::setRelativeTime ();
        logging::info (" Test commit 44f75 ", i, " start");
        utils::registerTest ("test_resources/commit_44f75/test" ~ std::conv::to!{[c8]} (i) ~ ".yr");
        logging::success (" Test commit 44f75 1 finished");
    }
}
